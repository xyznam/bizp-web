# 콘텐츠 만들기 정책

- 관련 화면: `03_콘텐츠만들기.html`(UW_CT_01), 연결: `02_콘텐츠검토.html`(UW_CR_01), `07_마케팅계획.html`(UW_PL_01), `16_마케팅전략상세.html`(UW_ST_06)
- 진입 경로: LNB "✏️ 콘텐츠 만들기"(L2·L3만 노출, §2), `01_홈.html`의 AI 마케팅 제안 카드 "만들어 보기 →"(M07, L1은 대행사 안내 카드로 대체)

## 1. 콘텐츠가 만들어지는 경로는 두 개다

이 화면은 **스케줄과 무관하게 지금 당장 만드는 즉석 생성** 경로다. 자동 생성 경로와는 별개로 다뤄야 한다.

| 구분 | 스케줄 자동 생성 | 즉석 생성(이 화면) |
|---|---|---|
| 시작점 | `07_마케팅계획`의 제안 주기대로 자동 | 사용자가 이 화면에서 직접 |
| 생성 시점 | 발행 며칠 전 AI가 알아서 | 버튼 누른 즉시 |
| 검수 흐름 | `02_콘텐츠검토`의 **검수 대기 큐**에 쌓임 → 나중에 몰아서 처리(대행사 일괄 검수 포함) | 승인권자가 본인이면 **큐를 건너뜀**(§4) |

즉석 생성분이 큐를 건너뛰는 이유: 만든 사람이 곧 승인권자인 상황(L2, L3+모드B)에서는 방금 미리보기로 확인하고 누른 것이 곧 검수이므로, 같은 사람이 다른 화면의 대기 목록에서 자기 글을 다시 찾아 승인하는 왕복이 의미가 없다. 반대로 스케줄 자동 생성분은 사람이 보지 않은 채 쌓이므로 큐가 반드시 필요하다.

## 2. 레벨별 노출

- **L1(위탁)**: LNB 메뉴에서 **"콘텐츠 만들기" 항목 자체를 숨긴다**. 콘텐츠 제작이 전부 대행사 소관이라 이 화면에서 할 수 있는 일이 없기 때문. URL로 직접 들어오는 경우를 위해 화면 안에는 "콘텐츠 만들기는 대행사 OO가 관리하고 있어요" 안내 카드(M03 fallback)를 그대로 둔다
- **L2(셀프)·L3(대행사)**: 위저드 전체 노출

메뉴 숨김은 `data-only-level="L2,L3"`로 처리한다. 원래 이 속성은 단일 레벨 정확일치만 지원해서 "L1만 제외"를 표현할 수 없었기 때문에, `assets/userweb.js`가 콤마 구분 목록을 받도록 확장했다(기존 단일값 표기는 그대로 동작).

## 3. 위저드 3스텝 + 생성 옵션

**① 어떤 소식인가요?** — 소식 유형 단일 선택.

- **선택지는 업종별 프리셋으로 정한다.** 매장이나 대행사가 직접 편집하는 값이 아니라, 업종을 정하면 그에 맞는 목록이 따라온다. 업종마다 알릴 만한 소식의 종류가 다르기 때문 — 음식점은 "신메뉴 출시"가 핵심이지만 의료·시술 업종엔 해당이 없고, 대신 "시술 후기"·"장비 도입" 같은 게 필요하다
- 프리셋 개념 자체는 `16_마케팅전략상세`가 이미 쓰고 있는 것과 같다(허용 편집 항목의 3단 티어, 검수 강도 강제 등이 업종 프리셋으로 결정됨 — `마케팅전략상세_정책.md` §6). 소식 유형도 같은 업종 프리셋 체계에 얹는다
- 현재 화면의 4개(신메뉴 출시 / 할인 이벤트 / 매장 이야기 / 계절 인사)는 **음식점 업종 프리셋의 예시**로 본다. 실제 프리셋 목록 정의와 업종별 매핑은 아직 없다(미해결 참조)
- `16_마케팅전략상세`의 "콘텐츠 소재 키워드"와는 다른 축이다 — 이쪽은 **글의 종류**(공지 유형, 업종 단위로 공통), 저쪽은 **매장별 구체 소재**(시술명·메뉴명 등, 매장마다 다름). 서로 대체하지 않는다

**② 한 줄로 알려주세요** — 자유 입력.

**③ 어디에 올릴까요?** — 채널 단일 선택(네이버 블로그 / 당근마켓). 선택한 채널명이 우측 미리보기 라벨에 반영된다.

**생성 옵션(M08)** — ③ 카드 안, 생성 버튼 위에 **인라인**으로 붙는다. 모달이 아니다.
- **번호를 붙이지 않는다**: ①②③은 생성에 반드시 필요한 필수 스텝이고, 생성 옵션은 안 건드려도 되는 선택 항목이라 성격이 다르다
- **정본 값을 다시 입력받지 않는다**: 말투·글 길이의 정본은 `16_마케팅전략상세`다. 여기서는 현재 매장 기본값을 **읽기 전용 요약**으로 보여주고, 정본을 고치러 가는 링크만 제공한다
- **링크 문구는 레벨에 따라 다르다**: L3는 "말투·길이 바꾸기 →", **L2는 "말투 바꾸기 →"** — L2 사장님 간단카드에는 말투만 있고 글 길이 항목이 없어서(글 길이는 L3 탭2 전용) L2에게 "길이"를 가리키면 가서 찾을 수 없는 걸 안내하게 된다
- **"이번 글만 다르게 쓸까요?"를 체크했을 때만** 오버라이드 패널이 열린다. 매장 기본값을 바꾸는 게 아니라 이 글 한 건에만 적용하는 조정이다
  - 말투: 단일 선택(친근하게 / 정중하게 / 발랄하게)
  - 길이: 단일 선택(짧게 400자 내외 / 보통 700자 내외 / 길게 1,000자 내외) — 글자수를 함께 표기해 감을 잡을 수 있게 한다
  - 꼭 넣을 키워드: 다중 선택. 자유 입력이 아니라 **매장 소재 키워드 중에서 고른다**(정본 재입력 방지)

## 4. 제출 = 승인권자에 따라 갈린다

승인권자 판정은 `02_콘텐츠검토`의 `reviewerOf()`와 **같은 기준(레벨 + 검수 모드)**을 쓴다. 검수 모드 A/B의 정의는 `콘텐츠검토_정책.md` §2 참조.

| 레벨·모드 | 승인권자 | 버튼 라벨 | 결과 |
|---|---|---|---|
| L2 | 본인(대행사가 없어 항상 모드 A) | 🚀 이대로 승인 완료 | 큐 생략, **승인 완료로 종료**(발행으로 간주) |
| L3 + 모드 B | 본인(대행사가 검수 주체) | 🚀 이대로 승인 완료 | 큐 생략, 등록 대기(대행사가 채널에 올린 뒤 URL 입력해 완료) |
| L3 + 모드 A | 사장님 | 🚀 이대로 사장님에게 승인 요청 | 사장님 차례로 넘어감 |

- 승인 완료 시 안내도 종착점에 따라 갈린다: **L2는 "[채널]에 올려주세요, 검토는 여기서 끝나요"**(완료 처리 단계가 없음), **L3+모드B는 "직접 등록한 뒤 완료 처리하세요"**(등록 대기로 넘어감). 어느 쪽이든 승인이 곧 발행은 아니다(`콘텐츠검토_정책.md` §1의 수동 발행 원칙)
- **제출 직후 버튼은 잠기고 완료형 라벨로 바뀐다**("✓ 승인 완료" / "✓ 승인 요청함"). 같은 초안을 두 번 내는 걸 막기 위함
- **새 초안이 만들어지면 다시 열린다**: "AI로 글 만들기", "다시 쓰기"를 누르거나 레벨·검수 모드가 바뀌면 초기 상태로 되돌아간다

## 미해결 / 미구현

- **소식 유형 업종별 프리셋 목록이 아직 정의되지 않았다**: 업종 프리셋으로 간다는 방향은 확정됐으나(§3), 업종을 몇 개로 나눌지·업종마다 어떤 유형을 넣을지는 아직 없다. 지금 화면의 4개는 음식점 프리셋 예시로 하드코딩된 상태이고, 업종을 바꿔도 목록이 따라 바뀌지 않는다. 프리셋 정의는 `16_마케팅전략상세`의 기존 업종 프리셋(허용 편집 항목·검수 강도)과 같은 곳에서 함께 관리되어야 한다
- **"직접 수정" 버튼이 실제로 편집을 못 한다**: 누르면 "본문을 눌러 바로 고칠 수 있어요" 토스트가 뜨지만 본문이 `contenteditable`이 아니라 눌러도 아무 일도 일어나지 않는다. 안내와 실제 동작이 어긋난 상태
- **위저드 선택이 초안에 반영되지 않는다**: 소식 유형·한 줄 입력·채널·생성 옵션을 무엇으로 고르든 우측 미리보기 내용은 고정값이다(프로토타입 한계). 채널명 라벨만 실제로 바뀐다
- **L1용 상단 부제 카피가 없다**: `UW_COPY.UW_CT_01.topbar.sub`에 L1 항목이 없어 L2 문구("고르기만 하면 AI가 글을 써드려요")가 대신 나온다. L1은 위저드 자체가 없어 사실과 어긋나는 문구지만, 메뉴에서 숨긴 뒤로는 URL 직접 진입 시에만 노출된다
- **"다시 쓰기"에 로딩 표현이 없다**: "AI로 글 만들기"는 뱃지가 "생성 중…"으로 바뀌는 연출이 있는데 "다시 쓰기"는 토스트만 뜨고 화면 변화가 없어 눌린 티가 약하다

## 변경 이력

- **2026-09-03 개편**: (1) 여러 매장 동시 생성(구 M07) 제거 (2) 제출 흐름을 승인권자 기준으로 재정의 — 이전엔 레벨만 보고 "🚀 이대로 검토 요청"(L2)/"사장님에게 승인 요청"(L3 고정)이었는데, L2는 자기가 만든 걸 자기한테 "요청"하는 꼴이었고 L3는 모드 B에서도 사장님 승인이 필요한 것처럼 안내하는 오류가 있었음. 승인권자가 본인이면 큐를 건너뛰고 바로 끝내도록 정리(§1·§4) (3) 생성 옵션을 모달에서 ③ 카드 인라인으로 옮기고 L3 전용에서 L2까지 확장, 내용도 "정본 재입력"에서 "요약 + 이번 글 한정 조정"으로 전환(§3) (4) L1 LNB 메뉴 비노출(§2) (5) 제출 후 버튼 잠금(§4)
- **소식 유형을 업종별 프리셋 소관으로 확정(2026-09-03)**: 처음엔 "이 4개가 어디서 오는지 정본이 없다"는 미해결 항목이었고, `16_마케팅전략상세`의 콘텐츠 소재 키워드에서 가져오는 안도 검토했으나 축이 달라(글의 종류 vs 매장별 소재) 부적절하다고 정리. 대신 업종마다 알릴 소식의 종류가 다르다는 점에서 이미 있는 업종 프리셋 체계에 얹는 것으로 확정(§3)
- **소식 유형 선택이 안 보이던 버그 수정(2026-09-03)**: JS는 `is-selected`를 정확히 토글하고 있었으나 `.uw-kw-chip`에 선택 상태 CSS가 아예 없어 화면상 변화가 없었음. 이 클래스는 다른 화면에서 "클릭해서 비활성화"(opacity 0.35) 용도로 쓰이고 있어 전역 클래스는 건드리지 않고, 이 화면의 `#topic-chips`에만 스코프한 선택 스타일을 추가
