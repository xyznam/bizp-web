# 마케팅 전략 상세 페이지 정책

- 관련 화면: `16_마케팅전략상세.html`(UW_ST_06), 연결: `06_우리매장정보.html`, `16_유사업체비교.html`(UW_ST_11), `02_콘텐츠검토.html`
- 진입 경로: 06 우리매장정보 › "마케팅 전략 상세" 카드(L3 전용 카드) 또는 사장님용 미리보기의 [더보기 →]

## 1. 레벨별 3가지 버전

같은 데이터(아래 §3 공용 필드 시스템)를 보여주는 방식이 레벨마다 다르다.

- **L1(위탁)**: 읽기 전용 카드. "위탁 매장은 조회만 가능해요. 수정은 담당 대행사가 진행해요" 안내, 칩 클릭·입력창·안내문구(rs-help) 전부 숨김
- **L2(셀프 사장님)**: 간단 카드 1장(8개 필드, §2). 탭도 근거도 AI 제안도 없이 "지금 값이 뭔지 + 손대면 사장님 수정으로 바뀐다"만 보여줌
- **L3(대행사)**: 탭 3개(우리 매장 분석 / 글쓰기 스타일 / 이미지 스타일)로 나뉜 풀버전. AI 분석 근거·AI 제안·예시 문구 관리 등 세부 편집 기능 전부 노출

## 2. L2 간단 카드 (8개 필드)

L3 탭2/3에서 쓰는 필드 중 사장님이 이해하기 쉬운 8개만 골라 하나의 카드로 압축한 것(같은 `data-*` 속성을 재사용해 L3와 값을 공유, §3 참조).

| 필드 | 유형 | 대응 L3 위치 |
|---|---|---|
| 말투 | 칩 | 탭2 문장과 표현 |
| 우리가 내세우는 강점 | 칩 | 탭1 우리 매장 분석 |
| 콘텐츠 소재 키워드 | 칩 | 탭1 우리 매장 분석 |
| 해시태그 | 칩 | 탭2 문장과 표현 |
| 쓰면 안 되는 말 | 칩(빨간 배지, 위험군) | 탭2 꼭 지킬 문구 |
| 피할 이미지 유형 | 칩(빨간 배지, 위험군) | 탭3 이미지 스타일 |
| 허용 편집 항목 | 3단 티어 표시(읽기전용, 대행사에게 문의 안내) | 탭3 이미지 스타일 |
| 검수 강도 | 텍스트(읽기전용) | 탭3 이미지 스타일 |

## 3. 공용 필드 시스템 (L2 간단카드 ↔ L3 탭이 값을 공유)

칩 형태 필드(말투/강점/키워드/해시태그/금칙어/피할이미지유형)는 `FIELD_DATA`/`FIELD_SRC`라는 하나의 메모리 값을 L2 카드와 L3 탭이 함께 참조한다. 화면이 달라도 같은 값, 같은 배지가 보인다.

- **배지 3종**: `AI 분석`(보라, 기본값) → `사장님 수정`(파랑, L2 간단카드에서 손댐) → `대행사 수정`(주황, L3 탭에서 손댐)
- **판정 기준**: 수정이 일어난 요소가 `.owner-simple-card`(L2 카드) 안이면 "사장님 수정", 그 밖(L3 탭)이면 "대행사 수정" — 배지는 화면 위치가 아니라 **누가 마지막으로 고쳤는가**를 값 하나로 전역 추적
- 칩 추가/클릭 비활성화 즉시 배지가 바뀌고, 다른 화면(L2 카드 또는 L3 탭)에 있는 같은 필드도 동시에 갱신됨
- 텍스트 필드(자리매김/대표시술/주손님 등 `<input>`/`<textarea>`형)는 이 공용 시스템 밖 — 개별 필드라 L2 간단카드에 대응 항목이 없음(탭1의 텍스트형 필드는 L3 전용)

## 4. L3 탭1 · 우리 매장 분석 (7개 필드)

자리매김 / 우리가 내세우는 강점 / 대표 시술 / 주 손님 / 콘텐츠 소재 키워드 / 리뷰에서 본 강점 / 리뷰에서 본 약점 — 전부 'AI 분석' 배지.

- **[근거] 링크**: 자리매김/대표시술/주손님/리뷰강점, 4개 필드에만 있음. 리뷰 약점에는 없음("약점은 글에 직접 쓰지 않고 보완 전략에만 참고한다"는 안내만)
- **근거 모달**: 4개 [근거] 링크가 전부 같은 모달(`#modal-evidence`)에 같은 고정 문구를 띄움(필드별 실제 근거 데이터는 없는 프로토타입 상태 — §미해결 참조)
- 하단에 "근처 비슷한 피부과와 비교해 볼까요?" 배너 → [비교해 보기] 클릭 시 `16_유사업체비교.html`(UW_ST_11)로 이동

## 5. L3 탭2 · 글쓰기 스타일 (18개 필드, 4개 그룹)

- **글의 뼈대**(7): 글의 목적 / 소재-키워드 맵 / 제목 패턴 / 도입부 패턴 / 본문 전개 구조 / 소제목 패턴 / 마무리 문구
- **문장과 표현**(6): 문장 스타일 / 글 길이 / 말투 / 해시태그 / 기호·이모지 / 트렌드 반영
- **검색 노출**(2): SEO 키워드 / 키워드 배치
- **꼭 지킬 문구**(3, 의료 등 특정 업종만 노출): 시작 문구 / 필수 푸터 문구(빨간 '필수' 배지, 법적 고정 문구) / 쓰면 안 되는 말

각 필드는 텍스트 직접 수정 + 파란 'AI 제안'(다른 방향 제안) 또는 회색 '현행 유지'(지금이 적절하다는 판단) 박스 중 하나가 붙는다. 제목/도입부/소제목 패턴 3개는 예시 문구를 여러 개 리스트로 관리(개별 추가/삭제).

## 6. L3 탭3 · 이미지 스타일 (11개 필드, 단일 카드)

베이스 이미지 소스 매칭 규칙 / 허용 편집 항목(3단 티어) / 인물·얼굴 노출(빨간 '필수', 의료광고법 고정) / 텍스트 오버레이 / 로고·워터마크 / 브랜드 아이덴티티 고정 규칙 / 검수 강도 / alt텍스트·파일명 / 레퍼런스 갱신 / 본문 이미지 배치 / 피할 이미지 유형.

- **허용 편집 항목 3단 티어**: 항상 허용(4개, 항상 ON) / 업종 프리셋에 따라 다름(9개, 업종별 기본 ON·OFF 다름) / 기본 OFF·특정 업종 잠금(2개: 인물 제거·인물 교체 — 자물쇠 표시, 매장이 임의로 켤 수 없음)
- **검수 강도가 06_우리매장정보 모드A/B를 제약함**: 이 필드가 "사장님 승인 필수"(업종 프리셋으로 강제, 완화 불가)를 나타내면, `06_우리매장정보.html`의 검수 방식 라디오에서 모드B(대행사 검수 후 발행, 사장님 승인 생략)를 선택 불가 처리하고 모드A(사장님 검수 후 발행)만 고를 수 있다. 승인 단계를 늘리는 방향(강화)은 자유, 승인 생략(완화)은 막혀 있는 편도 규칙. 상세는 `우리매장정보_정책.md` §5 참조

## 7. 브랜드 룰셋 상속 (프랜차이즈 전용, 데모 스위치)

헤더의 "매장 유형" 스위치(일반/프랜차이즈/브랜드 대표매장)는 **L3에서만 보이는 프로토타입 시연용 데모**(`title="프로토타입 전용"`)이지, 실제 매장 유형을 저장하는 기능이 아니다.

- **일반 매장**(기본값): 배지·토글 전부 없음 — 지금까지 서술한 동작 그대로
- **프랜차이즈 매장**: 탭마다 "브랜드 룰셋 상속" 토글이 나타남. ON(기본)이면 그 탭 전체가 입력 잠금(칩·텍스트·예시 추가 전부 비활성)되고 "브랜드 공통값을 따른다"는 뜻. OFF로 바꾸면 이 매장만의 값으로 편집 가능해짐
- **브랜드 대표매장**: 헤더에 "브랜드 대표 매장" 배지만 노출(상속 토글 없음 — 원본이라 상속받을 대상이 아님)
- 토글 클릭은 잠금 상태 전환 + 토스트만 하고, "상속 OFF 시점 값을 그대로 고정해서 넘겨받는" 실제 값 동기화 로직은 없음(목업 범위 밖, HTML 상단 주석에 명시)

## 8. 초기화 / 저장

헤더의 [초기화]는 확인 모달 후 "AI가 처음 분석한 값으로 되돌렸다"는 토스트만 띄움(실제 필드 값 되돌리기는 없음). [저장]도 "다음 콘텐츠부터 반영된다"는 토스트만 띄움 — 둘 다 실제 데이터 반영 없는 프로토타입 동작.

## 9. UID 보드 매핑

- `UW_ST_06`: L2 사장님 간단카드 기준 메인 보드(L1/L3는 요약 서술)
- `UW_ST_06__L3`: L3 탭1(우리 매장 분석) 실제 캡처
- `UW_ST_06__L3_style`: L3 탭2(글쓰기 스타일) 실제 캡처
- `UW_ST_06__L3_image`: L3 탭3(이미지 스타일) 실제 캡처
- 부속 모달 2개: `UW_ST_06__modal-reset`, `UW_ST_06__modal-evidence`

## 미해결 / 미구현

- **학습전 잠금 미적용**: `우리매장정보_정책.md` §1에 따르면 "학습전" 상태에서는 06 우리매장정보 화면에서 이 카드가 잠금 오버레이(`data-ob-lock`)로 덮인다. 그러나 이 화면(16_마케팅전략상세.html) 자체에는 그 게이트가 없다 — 06 화면을 거치지 않고 URL로 직접 들어오면 학습 전이어도 그대로 열림
- **근거 모달 데이터 없음**: §4의 [근거] 링크 4개가 전부 같은 고정 문구를 씀. 필드마다 다른 근거 텍스트를 보여주려면 데이터 구조 확장 필요
- **§6 검수 강도 제약 로직 미구현**: 정책만 확정, 06_우리매장정보의 라디오 비활성화 로직은 아직 반영 안 함
- **§6 검수 대상 범위 불명확**: 06 화면 캡션은 "AI가 만든 글"(텍스트)을 지칭하는데 이 필드는 "생성 이미지" 전용이라, 두 설정이 같은 대상을 가리키는지 범위가 불명확

## 변경 이력

- **탭2 필드 개수 정정**: 이전엔 "17개 필드"로 요약해 왔으나 실측 결과 18개가 정확한 개수였음(소재-키워드 맵, 소제목 패턴 2개가 요약에서 누락돼 있었음) — §5
- **§6 검수 강도-모드 A/B 관계, 방향 정정**: 처음엔 "06_우리매장정보가 정본이고 마케팅전략상세는 읽기전용 표시"로 정리했으나, 06의 모드B가 코드상 "사장님 승인 생략"이라 검수 강도(승인 필수)와 양립 불가능함을 확인해 방향을 정정함 — 마케팅전략상세의 검수 강도가 제약의 원천이고, 06의 선택지가 거기 맞춰 좁아지는 방향으로 바뀜
