# 마케팅 전략 상세 페이지 정책

- 관련 화면: `16_마케팅전략상세.html`(UW_ST_06), 연결: `06_우리매장정보.html`, `16_유사업체비교.html`(UW_ST_11), `02_콘텐츠검토.html`
- 진입 경로: 06 우리매장정보 › "마케팅 전략 상세" 카드(L3 전용 카드) 또는 사장님용 미리보기의 [더보기 →]

## 1. 레벨별 3가지 버전

같은 데이터(아래 §3 공용 필드 시스템)를 보여주는 방식이 레벨마다 다르다.

- **L1(위탁)**: 읽기 전용 카드. "위탁 매장은 조회만 가능해요. 수정은 담당 대행사가 진행해요" 안내, 칩 클릭·입력창·안내문구(rs-help) 전부 숨김
- **L2(셀프 사장님)**: 간단 카드 1장(8개 필드, §2). 탭도 근거도 AI 제안도 없이 "지금 값이 뭔지 + 손대면 사장님 수정으로 바뀐다"만 보여줌
- **L3(대행사)**: 탭 3개(우리 매장 분석 / 글쓰기 스타일 / 이미지 스타일)로 나뉜 풀버전. AI 분석 근거·AI 제안·예시 문구 관리 등 세부 편집 기능 전부 노출

## 2. L2 간단 카드 (9개 필드)

L3 탭2/3에서 쓰는 필드 중 사장님이 이해하기 쉬운 9개만 골라 하나의 카드로 압축한 것(같은 `data-*` 속성을 재사용해 L3와 값을 공유, §3 참조).

| 필드 | 유형 | 대응 L3 위치 |
|---|---|---|
| 말투 | 칩 | 탭2 문장과 표현 |
| 우리가 내세우는 강점 | 칩 | 탭1 우리 매장 분석 |
| 콘텐츠 소재 키워드 | 칩 | 탭1 우리 매장 분석 |
| 해시태그 | 칩 | 탭2 문장과 표현 |
| 쓰면 안 되는 말 | 칩(빨간 배지, 위험군) | 탭2 꼭 지킬 문구 |
| 피할 이미지 유형 | 칩(빨간 배지, 위험군) | 탭3 이미지 스타일 |
| 이미지 생성 여부 | 2단 선택 스위치 + 조건 칩 + 미리보기 버튼 | 탭3 이미지 스타일 |
| 허용 편집 항목 | 3단 티어 표시(읽기전용, 대행사에게 문의 안내) | 탭3 이미지 스타일 |
| 검수 강도 | 2단 선택 스위치(읽기전용, 잠금 표시만) | 탭3 이미지 스타일 |

**이미지 생성 여부(2026-09-03 신설)**: 대행사만 정할 일이 아니라 사장님도 원하는지 판단할 문제라서 L2 카드에도 동일하게 노출한 유일한 "편집 가능한" 신규 필드. [생성 미리보기] 버튼은 L1(읽기전용)에서도 클릭 가능 — 미리보기는 값을 바꾸지 않는 조회성 동작이라 §1의 읽기전용 원칙(칩·입력창·안내문구 숨김)에서 예외로 둠.

## 3. 공용 필드 시스템 (L2 간단카드 ↔ L3 탭이 값을 공유)

칩 형태 필드(말투/강점/키워드/해시태그/금칙어/피할이미지유형/대표시술/리뷰강점)는 `FIELD_DATA`/`FIELD_SRC`라는 하나의 메모리 값을 L2 카드와 L3 탭이 함께 참조한다. 화면이 달라도 같은 값, 같은 배지가 보인다.

- **배지 3종**: `AI 분석`(보라, 기본값) → `사장님 수정`(파랑, L2 간단카드에서 손댐) → `대행사 수정`(주황, L3 탭에서 손댐)
- **판정 기준**: 수정이 일어난 요소가 `.owner-simple-card`(L2 카드) 안이면 "사장님 수정", 그 밖(L3 탭)이면 "대행사 수정" — 배지는 화면 위치가 아니라 **누가 마지막으로 고쳤는가**를 값 하나로 전역 추적
- 칩 추가/클릭 비활성화 즉시 배지가 바뀌고, 다른 화면(L2 카드 또는 L3 탭)에 있는 같은 필드도 동시에 갱신됨
- **2026-09-03 확장**: 자유서술형(`<textarea class="rs-edit">`, `<textarea class="rs-example-text">`) AI 분석 필드도 직접 고치면 그 즉시 배지가 "대행사/사장님 수정"으로 바뀐다(칩형과 별도의 가벼운 감지 로직 — `FIELD_DATA`에 값이 들어가진 않고 배지 표시만 전환됨). 새로 만든 선택형 필드(레퍼런스 갱신·트렌드 반영·기호·이모지·텍스트 오버레이·로고·워터마크·글 길이·alt텍스트 우선순위)도 값을 바꾸면 같은 방식으로 배지가 전환된다 — §9 재학습이 이 필드들을 건드리지 않아야 함을 나타내는 신호로 씀
- **주의**: 대표 시술·리뷰에서 본 강점은 `FIELD_DATA`/`FIELD_SRC`를 쓰는 칩형이지만(위 목록), §2의 9개 필드 목록에는 없다 — L2 간단카드에 일부러 안 넣은 것뿐, 값 저장 방식 자체는 §2 필드들과 동일함
- 그 밖의 L3 전용 필드(자리매김/주손님/리뷰약점 등 탭1 나머지, 탭2 전체, 탭3의 새 선택형 필드 대부분)는 이 공유 시스템과 무관 — L3 탭 안에서만 존재하는 값

## 4. L3 탭1 · 우리 매장 분석 (7개 필드)

자리매김 / 우리가 내세우는 강점 / 대표 시술 / 주 손님 / 콘텐츠 소재 키워드 / 리뷰에서 본 강점 / 리뷰에서 본 약점 — 전부 'AI 분석' 배지.

- **[근거] 링크**: 자리매김/대표시술/주손님/리뷰강점, 4개 필드에만 있음. 리뷰 약점에는 없음("약점은 글에 직접 쓰지 않고 보완 전략에만 참고한다"는 안내만)
- **근거 모달**: 4개 [근거] 링크가 전부 같은 모달(`#modal-evidence`)에 같은 고정 문구를 띄움(필드별 실제 근거 데이터는 없는 프로토타입 상태 — §미해결 참조). 하단에 "마지막 학습" 날짜 한 줄이 붙어 있고(2026-09-03 신설), §9 재학습을 실행하면 이 날짜가 갱신됨
- 하단에 "근처 비슷한 피부과와 비교해 볼까요?" 배너 → [비교해 보기] 클릭 시 `16_유사업체비교.html`(UW_ST_11)로 이동

**필드별 형태(2026-09-03 정리)**:

| 필드 | 형태 | 비고 |
|---|---|---|
| 자리매김 | 텍스트(`<input>`) | |
| 우리가 내세우는 강점 | 칩(§3 공유 시스템) | |
| 대표 시술 | 칩(§3 공유 시스템) | 짧은 명사 나열이라 텍스트에서 칩으로 전환 |
| 주 손님 | 예시목록형(`rs-example-list`, 유형별 추가/삭제) | 항목당 문구가 길어서 칩 대신 제목 패턴과 같은 목록형 채택 |
| 콘텐츠 소재 키워드 | 칩(§3 공유 시스템) | |
| 리뷰에서 본 강점 | 칩(§3 공유 시스템) | |
| 리뷰에서 본 약점 | 텍스트, **읽기전용**(`readonly`) | 분석 결과를 보는 용도라 직접 수정 불가로 전환. 배지는 항상 'AI 분석' 고정, §9 재학습 대상에서도 구조적으로 제외됨 |

## 5. L3 탭2 · 글쓰기 스타일 (19개 필드, 4개 그룹)

- **글의 뼈대**(8): 헤더 정보 / 글의 목적 / 소재-키워드 맵 / 제목 패턴 / 도입부 패턴 / 본문 전개 구조 / 소제목 패턴 / 마무리 문구
- **문장과 표현**(6): 문장 스타일 / 글 길이 / 말투 / 해시태그 / 기호·이모지 / 트렌드 반영
- **검색 노출**(2): SEO 키워드 / 키워드 배치
- **꼭 지킬 문구**(3, 의료 등 특정 업종만 노출): 시작 문구 / 필수 푸터 문구(빨간 '필수' 배지, 법적 고정 문구) / 쓰면 안 되는 말

각 필드는 텍스트 직접 수정 + 파란 'AI 제안'(다른 방향 제안) 또는 회색 '현행 유지'(지금이 적절하다는 판단) 박스 중 하나가 붙는다. 제목/도입부/소제목 패턴 3개, 소재-키워드 맵(2026-09-03 전환)은 예시 문구를 여러 개 리스트로 관리(개별 추가/삭제).

**헤더 정보(2026-09-03 신설)**: 글 맨 위에 항상 붙는 헤더를 "사용 안 함 / 사진 / HTML" 중 하나로 고르는 선택 필드. AI 분석 대상이 아니라 배지는 '선택'(회색)으로 별도 표기.
- **사진**: 새로 업로드받지 않고 `99_매장자료관리.html`에 이미 등록된 사진 중에서 고른다(자료 정본은 그쪽 하나로 유지, [사진 바꾸기]가 해당 화면으로 이동). 이 화면 안에 별도 업로드 플로우 없음
- **HTML**: 배너·목차 위젯 등 직접 만든 HTML을 그대로 텍스트로 입력

**선택형으로 전환된 필드(2026-09-03)**:

| 필드 | 이전 형태 | 이후 형태 |
|---|---|---|
| 글 길이 | 텍스트 한 줄 | 짧게/보통/길게 3단 선택 + 세부 수치(700–1,000자 등)는 텍스트로 그대로 병기 |
| 기호·이모지 | 텍스트("최소로만 써요") | 안 씀/최소로/적극적으로 3단 선택 |
| 트렌드 반영 | 텍스트("중간 — ...") | 낮음/중간/높음 3단 선택 |

세 필드 모두 이미 문장이 강도어(중간/최소로 등)로 시작하던 값이라, 값을 바꾸려면 문장을 다시 써야 했던 이전 방식의 UX 문제를 선택형으로 해소함.

## 6. L3 탭3 · 이미지 스타일 (12개 필드, 단일 카드)

베이스 이미지 소스 매칭 규칙 / **이미지 생성 여부**(신설) / 허용 편집 항목(3단 티어) / 인물·얼굴 노출(빨간 '필수', 의료광고법 고정) / 텍스트 오버레이 / 로고·워터마크 / 브랜드 아이덴티티 고정 규칙 / 검수 강도 / alt텍스트·파일명 / 레퍼런스 갱신 / 본문 이미지 배치 / 피할 이미지 유형.

- **허용 편집 항목 3단 티어**: 항상 허용(4개, 항상 ON) / 업종 프리셋에 따라 다름(9개, 업종별 기본 ON·OFF 다름) / 기본 OFF·특정 업종 잠금(2개: 인물 제거·인물 교체 — 자물쇠 표시, 매장이 임의로 켤 수 없음)

**업종 프리셋이 결정하는 값 목록**: 이 화면의 허용 편집 항목·검수 강도 외에, `03_콘텐츠만들기`의 **소식 유형 선택지**도 업종 프리셋 소관이다(2026-09-03 확정). 업종마다 알릴 소식의 종류가 다르기 때문 — 상세는 `콘텐츠만들기_정책.md` §3. 프리셋 목록 자체의 정의·관리 위치는 아직 정해지지 않았고, 세 값이 같은 곳에서 함께 관리되어야 한다.

**이미지 생성 여부(2026-09-03 신설)**: "실사진만 사용 / 필요하면 생성" 2단 선택 + (생성 선택 시) 조건 칩 3종 + [생성 미리보기] 버튼. §2에서처럼 L2 간단카드에도 동일 노출. 조건 칩:
- 어울리는 실사진이 없을 때
- 계절·이벤트 배너가 필요할 때
- 설명용 그림(다이어그램·일러스트)이 필요할 때

인포그래픽 수치 금지 등 민감한 생성 규칙은 이 화면에 노출하지 않고 기존처럼 파일 하단 "백엔드 전용" 목록(HTML 주석)으로 비노출 유지 — 노출하면 매장이 "이 정도는 괜찮지 않냐"고 협상을 시도할 여지가 생긴다는 판단(⑨ 사실 왜곡 방지 규칙과 같은 맥락). [생성 미리보기]는 값을 바꾸지 않는 조회 동작이라 L1 읽기전용 상태에서도 클릭 가능.

**선택형으로 전환된 필드(2026-09-03)**:

| 필드 | 이전 형태 | 이후 형태 |
|---|---|---|
| 텍스트 오버레이 | 텍스트("넣지 않음") | 사용 안 함/사용함 2단 선택(사용함일 때 안내 문구 전환) |
| 로고·워터마크 | 텍스트("우측 하단에 삽입") | 사용 안 함/사용함 2단 선택 + 사용함일 때만 위치 드롭다운(4코너: 우/좌×상/하단) 노출 |
| 레퍼런스 갱신 | 텍스트("계속 편입해요") | 계속 편입/초기 레퍼런스 고정 2단 선택 |
| **검수 강도** | 텍스트("승인이 필요해요") | 승인 없이 자동 발행/🔒 발행 전 사장님 승인 필요 2단 선택. **이 매장(의료 업종)은 프리셋이 항상 켜야 하는 값으로 지정해서 스위치가 잠금(클릭 불가) 상태로 고정 노출** — 프리셋이 강제하지 않는 업종이면 이 잠금만 풀리는 구조. §2 L2 카드에도 같은 잠금 스위치 모양으로 동일 노출(읽기전용) |
| **alt텍스트·파일명** | 텍스트("메뉴명 → 매장명 → 지역명") | 1순위/2순위/3순위 드롭다운 3개(메뉴명/매장명/지역명). 같은 항목을 중복 선택하면 두 드롭다운이 자동으로 자리를 맞바꿈(값을 잃어버리지 않게) |

alt텍스트·파일명은 다른 선택형과 달리 "켜고 끄는" 값이 아니라 **순서 자체가 값**이라 드롭다운 3개 조합으로 구현했다(체크박스·토글로는 표현 불가).

**검수 강도가 06_우리매장정보 모드A/B를 제약함**: 이 필드가 "사장님 승인 필수"(업종 프리셋으로 강제, 스위치 잠금)를 나타내면, `06_우리매장정보.html`의 검수 방식 라디오에서 모드B(대행사 검수 후 발행, 사장님 승인 생략)를 선택 불가 처리하고 모드A(사장님 검수 후 발행)만 고를 수 있다. 승인 단계를 늘리는 방향(강화)은 자유, 승인 생략(완화)은 막혀 있는 편도 규칙. 상세는 `우리매장정보_정책.md` §5 참조. (이번 스위치 UI 전환으로 값의 의미는 바뀌지 않았음 — 여전히 on/off 이진값, 표현 방식만 텍스트→스위치)

## 7. 브랜드 룰셋 상속 (프랜차이즈 전용, 데모 스위치)

헤더의 "매장 유형" 스위치(일반/프랜차이즈/브랜드 대표매장)는 **L3에서만 보이는 프로토타입 시연용 데모**(`title="프로토타입 전용"`)이지, 실제 매장 유형을 저장하는 기능이 아니다.

- **일반 매장**(기본값): 배지·토글 전부 없음 — 지금까지 서술한 동작 그대로
- **프랜차이즈 매장**: 탭마다 "브랜드 룰셋 상속" 토글이 나타남. ON(기본)이면 그 탭 전체가 입력 잠금되고 "브랜드 공통값을 따른다"는 뜻. OFF로 바꾸면 이 매장만의 값으로 편집 가능해짐
- **브랜드 대표매장**: 헤더에 "브랜드 대표 매장" 배지만 노출(상속 토글 없음 — 원본이라 상속받을 대상이 아님)
- 토글 클릭은 잠금 상태 전환 + 토스트만 하고, "상속 OFF 시점 값을 그대로 고정해서 넘겨받는" 실제 값 동기화 로직은 없음(목업 범위 밖, HTML 상단 주석에 명시)
- **잠금 대상(2026-09-03 확장)**: 텍스트(`.rs-edit`)·`<textarea>`·`<select>`·체크박스가 아닌 `<input>`·예시 추가/삭제 버튼·칩(`[data-kw-chip]`)에 더해, 이번에 새로 만든 선택형 스위치(`.rs-mini-switch button`)와 이미지 생성 조건 칩(`.rs-cond-chip`)도 잠금 대상에 포함시킴 — 안 그러면 탭 전체가 잠겼는데 새 선택형 필드만 계속 클릭되는 구멍이 생기기 때문

## 8. 재학습 / 이전 학습으로 되돌리기 (헤더, 전 탭 공통, 2026-09-03 신설)

기존 [초기화](최초 분석값으로 무조건 복귀)를 없애고 두 버튼으로 교체했다.

- **[↻ 재학습]**: 사장님·대행사가 직접 고친 필드(배지가 'AI 분석'이 아닌 것)는 그대로 두고, 아직 'AI 분석' 상태인 필드만 최신 콘텐츠 기준으로 다시 분석한다. 확인 모달을 거쳐 실행되고, §4 근거 모달의 "마지막 학습" 날짜가 갱신된다
  - `15_AI학습현황.html`의 "다시 학습하기"와는 다른 개념 — 그쪽은 콘텐츠 자체를 다시 수집(+학습)하고, 이 화면의 재학습은 **이미 수집된 콘텐츠로 전략값만 다시 뽑아내는 것**(콘텐츠 재수집 없음)
  - 프로토타입 한계: 실제로 값이 바뀌는 건 칩형 필드(말투·강점·키워드·해시태그·금지어·피할이미지) 6종뿐. 자유서술형 AI 분석 필드는 텍스트를 새로 쓰지 않고 학습일만 갱신됨(실서비스에서는 자유서술형도 새 텍스트로 갱신되는 게 맞음)
- **[이전 학습으로 되돌리기]**: "재학습을 한 번 눌러야 되돌릴 게 생기는" 세션 스코프 버튼이 아니다 — 지금 보이는 값도 이미 과거 어느 시점 학습 결과이므로, **페이지를 처음 열었을 때부터 활성 상태**이고 그 이전 시점 스냅샷으로 되돌린다. 되돌린 뒤에는 비활성화되고, 재학습을 다시 실행하면 그 시점이 새 되돌리기 대상이 되며 다시 활성화된다(항상 1단계 전까지만 되돌리는 구조)

## 9. 저장

헤더의 [저장]은 "다음 콘텐츠부터 반영된다"는 토스트만 띄움 — 실제 데이터 반영 없는 프로토타입 동작.

## 10. UID 보드 매핑

- `UW_ST_06`: L2 사장님 간단카드 기준 메인 보드(L1/L3는 요약 서술)
- `UW_ST_06__L3`: L3 탭1(우리 매장 분석) 실제 캡처
- `UW_ST_06__L3_style` / `UW_ST_06__L3_style_b` / `UW_ST_06__L3_style_c`: L3 탭2(글쓰기 스타일) 실제 캡처. 2026-09-03에 Figma `createImage`의 `Image is too large` 실패(원본 1762×7630px, 4096px 한도 초과)로 3개 보드로 분할함 — `_style`은 '글의 뼈대' 카드 앞 4행, `_style_b`는 뒤 4행, `_style_c`는 나머지 3개 카드(문장과 표현/검색 노출/꼭 지킬 문구). 캡처 시 같은 탭 안에서 필요 없는 카드·행을 `display:none`으로 숨겨 잘라내는 기법 사용(실제 화면에는 이런 분할이 없음, 캡처 전용)
- `UW_ST_06__L3_image`: L3 탭3(이미지 스타일) 실제 캡처
- 부속 모달 3개: `UW_ST_06__modal-relearn`(2026-09-03, `modal-reset`에서 개명), `UW_ST_06__modal-evidence`, `UW_ST_06__modal-image-preview`(2026-09-03 신설)

## 미해결 / 미구현

- **학습전 잠금 미적용**: `우리매장정보_정책.md` §1에 따르면 "학습전" 상태에서는 06 우리매장정보 화면에서 이 카드가 잠금 오버레이(`data-ob-lock`)로 덮인다. 그러나 이 화면(16_마케팅전략상세.html) 자체에는 그 게이트가 없다 — 06 화면을 거치지 않고 URL로 직접 들어오면 학습 전이어도 그대로 열림
- **근거 모달 데이터 없음**: §4의 [근거] 링크 4개가 전부 같은 고정 문구를 씀. 필드마다 다른 근거 텍스트를 보여주려면 데이터 구조 확장 필요
- **§6 검수 강도 제약 로직 미구현**: 정책만 확정, 06_우리매장정보의 라디오 비활성화 로직은 아직 반영 안 함
- **§6 검수 대상 범위 불명확**: 06 화면 캡션은 "AI가 만든 글"(텍스트)을 지칭하는데 이 필드는 "생성 이미지" 전용이라, 두 설정이 같은 대상을 가리키는지 범위가 불명확

## 변경 이력

- **탭2 필드 개수 정정**: 이전엔 "17개 필드"로 요약해 왔으나 실측 결과 18개가 정확한 개수였음(소재-키워드 맵, 소제목 패턴 2개가 요약에서 누락돼 있었음) — §5
- **§6 검수 강도-모드 A/B 관계, 방향 정정**: 처음엔 "06_우리매장정보가 정본이고 마케팅전략상세는 읽기전용 표시"로 정리했으나, 06의 모드B가 코드상 "사장님 승인 생략"이라 검수 강도(승인 필수)와 양립 불가능함을 확인해 방향을 정정함 — 마케팅전략상세의 검수 강도가 제약의 원천이고, 06의 선택지가 거기 맞춰 좁아지는 방향으로 바뀜
- **2026-09-03 대규모 개편**: (1) 우리 매장 분석 탭 헤더의 [초기화]를 [재학습]+[이전 학습으로 되돌리기]로 교체 — "직접 안 고친 값만 재분석"하는 개념으로, AI학습현황의 콘텐츠 수집과는 분리. 되돌리기는 세션 스코프가 아니라 페이지를 열 때부터 항상 활성 상태로 설계(사용자 피드백으로 최초 설계를 수정 — "되돌리기는 마지막 학습 전으로 되돌리는 것이라 재학습을 먼저 해야 활성화되는 게 아니다"). 근거 모달에 마지막 학습 날짜 추가 (2) 대표 시술·리뷰에서 본 강점을 칩으로, 주 손님을 예시목록형으로, 리뷰에서 본 약점을 읽기전용으로 전환 (3) 헤더 정보 필드 신설(사진은 매장자료관리에서 선택, 새 업로드 없음) (4) 자유서술형이던 여러 값을 선택형 UI로 전환 — 레퍼런스 갱신·기호·이모지·트렌드 반영·텍스트 오버레이·로고·워터마크·글 길이·검수 강도(2단 스위치, 업종 강제 시 잠금)·alt텍스트·파일명(1/2/3순위 드롭다운) (5) 이미지 생성 여부 필드 신설, L2 사장님 카드에도 동일 노출(대행사만 정할 문제가 아니라는 판단), [생성 미리보기] 모달 신설 (6) 브랜드 룰셋 상속 잠금 CSS에 새 선택형 컨트롤(`select`, `.rs-mini-switch button`, `.rs-cond-chip`)을 포함하도록 확장 — 안 그러면 탭이 잠겼는데 새 필드만 계속 클릭되는 구멍이 생김
