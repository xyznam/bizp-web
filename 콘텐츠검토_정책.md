# 콘텐츠 검토 정책

- 관련 화면: `02_콘텐츠검토.html`(UW_CR_01), `02_발행이력.html`(UW_CR_02), `06_우리매장정보.html`(검수 방식 설정), `16_마케팅전략상세.html`(룰셋)

## 배경

발행 API가 없어서 승인해도 자동으로 채널에 올라가지 않는다. 이 제약이 검수 흐름 전체의 전제가 된다. 레벨은 L1 위탁(사장님, 대행사에 위탁) / L2 셀프(사장님 본인) / L3 대행사(직원 화면) 세 가지.

## 1. 발행은 항상 수동이다

네이버 블로그·당근마켓 둘 다 승인 후 **사람이 채널에 직접 등록**해야 한다. "자동발행", "예약 발행" 개념은 없음.

**등록 대기 단계는 등록 실무자가 따로 있을 때만 둔다(= 대행사가 올려주는 경우).**

| 레벨 | 승인하면 | 등록 실무자 | 등록 대기 단계 |
|---|---|---|---|
| **L2 셀프** | **"승인 완료"로 검토 종료** | 사장님 본인 | **없음** |
| L1 위탁 | 등록 대기 | 대행사 | 있음(사장님은 기다리기만, 카드는 대행사 화면에만 노출) |
| L3 대행사 | 등록 대기 | 대행사 본인 | 있음 |

L2에서 단계를 없앤 이유: 승인한 사람과 채널에 올릴 사람이 같은 사람이라, 자기가 올릴 글을 자기 할 일 목록에서 다시 찾아 완료 처리하는 왕복이 의미가 없다. `03_콘텐츠만들기`에서 "승인권자가 본인이면 큐를 건너뛴다"고 정한 것과 같은 논리(`콘텐츠만들기_정책.md` §1).

**L2의 "승인 완료"는 발행된 것으로 간주한다.** 발행 여부를 확인해주는 단계가 없어졌으므로, `02_발행이력`·`08_실행결과리포트`·`01_홈`의 발행 집계는 L2 매장의 "승인 완료"를 발행 완료와 같이 센다. 실제로 채널에 올리지 않아도 집계되는 오차는 감수한다(이 오차를 없애려면 완료 확인 단계를 되살려야 하는데, 그게 없애려던 왕복이라 맞바꾼 것).

## 2. 검수 모드는 매장당 A/B 둘 중 하나

- **모드 A (사장님 검수형)**: AI 생성 → 사장님이 승인/반려. 대행사가 있어도 대행사는 실행·등록만 함
- **모드 B (대행사 위임형)**: AI 생성 → 대행사가 검수 → 바로 등록 대기로. 사장님 검수 자체가 없음
- **L2 셀프는 대행사가 없으므로 항상 모드 A.** 모드 B는 위탁 매장(L1↔L3)에서만 선택 가능
- 사전검수/이중검수 같은 다른 조합은 없음 — 매장당 이 2택뿐

**설정 위치**: `06_우리매장정보.html`의 "콘텐츠 검수·발행 방식" 섹션(10_온보딩에서 최초 선택). `02_콘텐츠검토`에는 읽기전용 배지 + 06 바로가기만 있음.

## 3. 상태 모델 (정식 5개 + 임시 1개)

레벨에 따라 종착점이 다르다.

- **L2 셀프**: 검수 대기 → (반려 | **승인 완료**) ← 여기서 끝
- **L1·L3 위탁**: 검수 대기 → (반려 | 등록 대기) → 발행 완료

그 사이에 임시 상태로 **다시 쓰는 중**이 있음.

- **"승인 완료"는 L2 전용 종착 상태**다(§1). L1·L3에는 이 상태가 없고, 반대로 **L2에는 "등록 대기"가 없다** — 화면 목록에서도 걸러서 보여주지 않는다
- "승인 대기"는 별도 상태가 아니라 검수 대기의 라벨 변형(모드A·대행사 화면에서는 "사장님 승인 대기"로 표기)

- 당근마켓의 "발송 완료"도 "발행 완료"로 통일

**이 화면의 검수 대기 큐는 `07_마케팅계획`의 제안 주기대로 자동 생성된 콘텐츠가 채운다.** `03_콘텐츠만들기`에서 즉석으로 만든 콘텐츠는 승인권자가 본인일 때(L2, L3+모드B) 검수 대기를 건너뛴다 — 방금 미리보기로 확인하고 누른 것이 곧 검수이기 때문. 이때 도착지는 위 종착점 규칙을 그대로 따라서 **L2는 승인 완료, L3+모드B는 등록 대기**다. 즉석 생성분이라도 승인권자가 따로 있으면(L3+모드A=사장님) 평소대로 검수 대기(사장님 차례)로 들어온다. 상세는 `콘텐츠만들기_정책.md` §1·§4 참조.

### 검수 주체(reviewer) 라우팅

- 첫 초안: 모드로 결정(A=사장님, B=대행사)
- 반려 후 재작업: 재작업을 지시한 레벨이 다음 검수 주체 결정 — **L3에서 재작업하면 대행사가 먼저 자체 확인**(agency) → "✓ 확인·사장님께 승인 요청"으로 사장님(owner)에게 넘김. L2는 본인이 바로 검수 주체
- 다시쓰기/반려 재작업 시 **3초 후 새 초안이 "검수 대기"로 복귀**(제목에 "(수정본)" 표시, 점수 +7 — 코드에 하드코딩된 값, `doc.score = Math.min(99, score+7)`)
- 근거: 대행사가 이미 개입한 글의 자기 QA일 뿐 전수 사전검수가 아니므로 병목이 없음(반려 핑퐁 방지)

## 4. 승인/반려 버튼

| 레벨·모드 | 승인 버튼 라벨 | 누르면 가는 상태 |
|---|---|---|
| L2 | 승인 | **승인 완료(종료)** |
| L1 모드A | 승인 | 등록 대기(대행사가 올림) |
| L3 모드A (재작업 확인) | ✓ 확인 · 사장님께 승인 요청 | 검수 대기(사장님 차례) |
| L3 모드B | ✓ 검수 완료 · 발행 등록 | 등록 대기 |

- **반려는 승인권자(사장님=L1·L2)에게만 있음. 대행사(L3)에는 없음** — 대행사는 승인권자가 아니므로
- L1 사장님의 반려 → **대행사 검토 큐에 노출**(반려 사유 포함). L2 반려 → 곧바로 AI 다시쓰기로 직결

## 4-1. 발행 완료 처리 시 글 주소 입력

등록 대기 목록(대행사 화면)에서 [발행 완료 처리]를 누르면 **발행한 글 주소를 입력받는 모달**이 뜬다. 여기서 받은 URL은 `08_실행결과리포트`에서 조회수 같은 통계를 글 단위로 매칭하는 데 쓴다.

- **네이버 블로그: URL 필수.** 입력하기 전까지 완료 버튼이 잠긴다
- **당근마켓: 받지 않음.** 글 URL 개념이 블로그와 달라서 입력란 자체를 띄우지 않고 바로 완료 처리한다
- L2에는 이 단계 자체가 없다(§1). 즉 **L2 셀프 매장은 발행 URL을 수집하지 않는다** — 리포트에서 글 단위 매칭은 위탁 매장(L1·L3)에서만 가능하다는 뜻

## 5. 반려와 AI 다시쓰기는 역할이 다르다

- **반려**: 승인 거부 + 사유 기록만. 재생성 지시가 아님
- **AI 다시쓰기**: "무엇을 바꿀지" 입력받는 별도 통합 시트(주제 변경/톤 조정/자유 지시). 반려 모달에는 "직접 쓸게요" 칩을 넣지 않음

## 6. 대행사(L3) 검토 큐 = 3그룹

승인/반려는 "상태"가 아니라 **"대행사에게 떨어진 할 일"**이라는 원칙:

1. **내가 처리할 일**: 사장님이 반려한 글의 재작업 / 사장님이 승인한 글의 등록
2. **사장님 승인 대기중**
3. **발행 완료 이력**

카드엔 매장명·채널·상태 배지·반려 사유(L1 반려 시)·타임스탬프를 표시. 일괄승인(M07)은 모드B 매장에만 해당. 모드B에서 사장님(L1) 화면은 읽기전용("대행사가 검수·발행 중").

## 7. 타깃 키워드

필드명은 "타깃 키워드" + 툴팁("이 글이 이 검색어로 노출되길 노리고 작성됨").

## 8. 발행 완료 이력

발행 완료 이력은 `02_발행이력.html`(UW_CR_02)에서 별도 화면으로 관리한다 — 자체 목데이터, 상태(발행완료/반려)·채널·기간 필터, 제목 검색, L3 전용 매장 필터 포함. `02_콘텐츠검토`의 "최근 처리한 콘텐츠" 카드 하단 "더보기 →"로 진입. 본문·점수 상세 보기 클릭 진입은 스코프 밖(후속 과제).

**L2 매장의 이력은 "승인 완료" 건으로 채워진다**(§1의 발행 간주 규칙). 상태 필터가 발행완료/반려 2택인 지금 구조에서는 L2의 승인 완료를 "발행 완료"로 묶어서 보여줘야 한다(미해결 참조).

## 9. HTML/CSS 학습 기반 콘텐츠 (설계만 확정, 생성 기능 자체는 별도 과제)

- 학습된 디자인(CSS) 미리보기는 **인라인 금지, 별도 모달**(실제 발행 폭 ~700px 기준 iframe). 이유: 검토 패널 폭과 실제 발행 폭이 달라 인라인이면 폭이 왜곡되고, 매장별 CSS를 인라인 주입하면 앱 스타일과 충돌 위험
- 기본 상세 뷰는 텍스트 그대로 유지(L3가 여러 매장 글을 빠르게 훑어보는 게 우선)
- **이미지는 본문 안에 제자리 배치 가능**(디자인 충돌과 무관 — 앱 자체 스타일로 감싸면 안전). 본문 데이터는 통짜 텍스트 대신 구조화된 블록 배열(`[{type:"p", text}, {type:"img", src}, ...]`)이어야 함
- 이미지는 16_마케팅전략상세의 "베이스 이미지 소스 매칭 규칙"을 따르되, 데모에서는 플레이스홀더 사용 확정

## 미해결 / 확정만 되고 미구현

- **검색 노출 점수 배점표**: 16_마케팅전략상세 룰셋엔 정성 규칙(SEO 키워드, 700~1000자, 이미지 300자당 1장, 소제목 3~5개 등)만 있고 배점 산출 로직이 없음(현재 87점 하드코딩). "왜 이 점수" 기능을 제대로 하려면 배점표 신규 정의 필요. 점수의 성격은 "검색순위 보장"이 아니라 **"룰셋 준수도"**로 명시할 것
- **채널별 점수 지표 분리**: 당근마켓엔 "검색 노출 점수"라는 이름이 안 맞음(지역 노출 성격) — 배점표 과제와 묶어서 처리
- **직접 편집 버튼(M05)**: 지금은 토스트만 뜨는 껍데기, 실제 구현은 후순위. `03_콘텐츠만들기`의 "직접 수정" 버튼도 같은 상태라 두 화면 모두 동작하지 않는 편집 버튼을 달고 있음
- **L2 승인 완료를 발행 집계에 반영하는 로직**: §1에서 "발행으로 간주"로 정했지만 `02_발행이력`·`08_실행결과리포트`·`01_홈`은 각자 목데이터를 쓰는 프로토타입이라 실제로 연결된 곳이 없음. 각 화면이 상태를 실제로 읽게 될 때 "승인 완료도 발행으로 센다"를 반영해야 함
- **발행 URL의 쓰임새**: 입력받은 URL을 실제로 저장·활용하는 곳이 아직 없음(화면 안 메모리에만 담김). `08_실행결과리포트`에서 글 단위 조회수를 매칭할 때 연결 필요

## 변경 이력

- **등록 대기 단계 범위 확장**: 이전엔 L3 전용으로 잘못 좁혀져 있었는데, L2 셀프에도 필요하다는 걸 확인해 전 레벨로 확장(§1) — **2026-09-03에 다시 L2 제외로 정정됨(아래 참조)**
- **검수 모드 설정 위치 이전**: 이전엔 `02_콘텐츠검토` 안의 숨은 `⚙ 검수 기준` 모달이었으나, `06_우리매장정보`의 "콘텐츠 검수·발행 방식" 섹션으로 옮김(§2)
- **필드명 개명**: "대표 키워드" → "타깃 키워드"(§7). 룰셋에 콘텐츠 소재 키워드/SEO 키워드/해시태그 3종이 이미 있어 "대표"라는 이름이 모호했기 때문
- **발행 완료 이력 화면 분리**: `02_발행이력.html`(UW_CR_02)을 신설(§8). 이전엔 `02_콘텐츠검토` 화면 안에서 상태 전환으로만 보여줬음
- **즉석 생성분의 검수 대기 우회 명시(2026-09-03)**: `03_콘텐츠만들기` 개편으로 "승인권자가 본인이면 검수 대기를 건너뛴다"는 예외가 생겨 §3에 반영. 이 화면의 큐·일괄 검수(M07)는 스케줄 자동 생성분 전용이라는 점이 이때 분명해짐(즉석 생성 경로와 혼동해 M07을 죽은 기능으로 오판했다가 정정)
- **L2 등록 대기 폐지, "승인 완료" 종착 상태 신설(2026-09-03)**: 앞서 "L2에도 등록 대기가 필요하다"고 확장했던 결정을 뒤집은 것. 같은 사실(사장님이 직접 올려야 함)에서 그때는 "그러니 할 일 목록이 필요하다", 이번엔 "그러니 자기 목록에서 자기 글을 다시 확인하는 왕복이 불필요하다"로 결론이 갈렸고, 후자로 확정. 대신 발행 여부를 확인해주는 신호가 사라져서 **"L2 승인 완료 = 발행으로 간주"** 규칙을 함께 못박음(§1). 화면에서도 L2일 때는 등록 대기 건을 목록에서 아예 걸러낸다
- **발행 완료 처리 시 글 주소 입력 구현(2026-09-03)**: 미해결에 있던 항목을 구현하면서 조건이 바뀜 — 원래는 "나중 입력 허용(선택, 비차단)"이었으나 **네이버 블로그는 필수(입력 전까지 완료 버튼 잠금)**로 강화. 당근마켓 생략은 그대로(§4-1). 아울러 등록 대기 목록(M09)이 정적 1건 하드코딩이라 승인해도 목록에 안 잡히던 것을 실제 데이터 기반 렌더링으로 교체
- **M05 라벨 개명(2026-09-03)**: "문구 수정" → "직접 편집"
- 확정 시점: 2026-08-12~13, 6차에 걸친 논의로 정리됨
