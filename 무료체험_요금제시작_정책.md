# 무료 체험 · 요금제 시작 정책

- 관련 화면(예정): `09_요금제시작.html`(신설 예정, UW_BL_08 가칭)
- 관련 화면(기존): `21_회원가입.html`(UW_ON_05), `10_온보딩.html`(UW_ON_01), `98_매장설정.html`(UW_ST_08), `01_홈.html`(UW_HM_01), `01_홈_미결제.html`(UW_HM_05), `09_요금제비용.html`(UW_BL_01), `09_담당매장청구.html`(UW_BL_05), `18_알림센터.html`(UW_MY_02)
- **화면은 아직 만들지 않았다.** 이 문서는 규칙만 확정한 것이고, 구현 시 이 문서를 기준으로 한다

## 배경

가입 직후 구독을 시작할 화면이 없다. 요금제 관련 기존 화면 셋은 전부 "이미 구독 중"을 전제한다 — `09_요금제비용`은 상태가 `현재 이용 중`과 `만료 임박` 둘뿐이고, `25_요금제변경`은 지금 요금제 대비 차액을 계산해 다음 갱신일부터 적용하며, `26_결제문제`는 결제 실패 후 재시도다. 최초 결제 경로가 비어 있다.

반면 대행사 화면에는 `미개시` 상태가 이미 있다(`09_담당매장청구` KPI 4종, `billing-store.js` 담당 78곳 중 미개시 4곳). 대행사는 아직 시작하지 않은 매장을 세고 있는데 그 매장의 사장님이 시작할 화면이 없는 상태다.

## 1. 적용 범위는 L2 셀프뿐이다

| 레벨 | 체험·요금제 시작 |
|---|---|
| L1 위탁 | 해당 없음 — 대행사가 대납하므로 사장님이 요금제를 고르지 않는다 |
| **L2 셀프** | **해당** — 본인이 고르고 본인이 결제한다 |
| L3 대행사 | 해당 없음 — 자기 구독이 아니라 담당 매장 구독을 `09_담당매장청구`에서 다룬다 |

L3 화면에서는 담당 매장의 체험 상태를 **조회**한다(§7).

## 2. 체험 조건

- **기간 7일.** **시작 시점은 가입 완료 시각**이고, 종료는 시작 + 7일의 같은 시각. 사장님이 따로 시작 버튼을 누르지 않는다
- **결제 수단을 받지 않는다.** 카드 등록 없이 시작한다. 따라서 만료 시 자동 결제도 없고, 만료는 곧 잠금이다
- **기능은 전부 연다.** 체험 중 기능을 단계적으로 잠그지 않는다. 가치를 체감시키는 것이 체험의 목적이라 반대로 간다
- **단 하나의 예외는 문자다.** 비즈챗 문자는 발송당 실비가 나가므로 **체험 기간 통틀어 100건**으로 제한한다. 소진하면 그 시점부터 발송이 막히고, 남은 체험 기간에도 회복되지 않는다
  - **세는 기준은 발송 성공이다.** 실패분은 차감하지 않는다. `09_요금제비용`의 사용량 집계와 같은 기준이라 두 화면이 다른 답을 주지 않는다
  - 실패 건을 다시 보내 성공하면 그때 1건으로 센다
- 체험은 매장당 1회다. 해지 후 재가입해도 다시 주어지지 않는다

체험을 가입 완료에 붙인 이유는 **중간 이탈을 상태 공백으로 만들지 않기 위해서**다. 온보딩이나 매장 설정을 하다 나가도 이미 체험 중이라, 다시 들어왔을 때 홈이 정상으로 뜨고 하던 자리에서 이어간다. 시작 버튼을 따로 두면 그 버튼을 누르기 전에 이탈한 사람의 상태를 정의해야 하는데, 카드도 안 받는 마당에 그 단계를 둘 이유가 없다.

## 3. 가입부터의 순서

```
21_회원가입 ──[체험 시작]── 10_온보딩 → 09_요금제시작 → 98_매장설정 → 01_홈
```

- **체험은 가입 완료와 동시에 시작된다.** 이후 화면들은 이미 체험 중인 상태에서 지나간다
- 온보딩이 먼저다. 온보딩 마지막이 AI 학습 시작이라 가치를 체감한 직후에 요금제를 만나게 된다
- `09_요금제시작`은 **체험을 여는 화면이 아니라 요금제를 정하는 화면**이다. 여기서 하는 일은 둘이다
  - **요금제 미리 고르기** — 결제 없이 선택만 저장한다(§5)
  - **바로 결제** — 체험을 기다리지 않고 지금 시작한다
  - 둘 다 건너뛸 수 있다. 건너뛰어도 체험은 이미 돌아가고 있다
- 매장 설정은 그 뒤다. 매장 정보가 있어야 AI가 배울 것이 생기고, 챗봇도 매장 정보가 없으면 만들 수 없다(`05_AI챗봇`의 매장정보 미등록 상태)

**어느 단계에서 이탈하든 상태는 체험 중이다.** 다시 들어오면 홈이 정상으로 뜨고, 온보딩이나 매장 설정을 마치지 않았다면 그 자리로 유도한다.

## 4. 구독 상태 모델

기존 4종에 `체험 중`을 더해 5종으로 한다.

| 상태 | 뜻 | 사장님 화면 |
|---|---|---|
| **체험 중** | 가입 완료 후 7일 안 | 전 기능 개방 + 잔여일 표시 |
| 구독 중 | 결제 완료 | 정상 |
| 만료 임박 | 갱신일 D-7 이내 | 정상 + 안내 |
| 연체 | 갱신일 경과, 결제 실패 | `01_홈_미결제` |
| 미개시 | 체험이 끝났고 결제도 하지 않음 | `01_홈_미결제` + `09_요금제시작`으로 유도 |

- 체험이 가입과 동시에 시작하므로 **L2 사장님에게 미개시는 체험 이전이 아니라 체험 이후의 상태다.** 가입만 하고 아무것도 안 한 구간은 존재하지 않는다
- 체험 종료 시 상태는 **연체가 아니라 미개시다.** 낼 돈이 밀린 것이 아니라 아직 시작하지 않은 것이기 때문이다
- 화면은 `01_홈_미결제`를 공유한다. 그 화면의 L2 영역이 이미 "요금제 시작 + 쓸 수 있는 기능 안내"라 체험 종료 화면으로 그대로 쓸 수 있다
- `09_담당매장청구`의 `미개시`는 대행사가 아직 인수하지 않은 매장도 포함한다. L2 사장님 화면의 미개시와 뜻이 겹치지 않으니 라벨을 공유하되 계산은 각 화면 기준으로 한다

## 5. 요금제 미리 고르기

체험 중에 요금제를 **결제 없이 골라 둘 수 있다.** 종료 시 결제 단계만 남기기 위한 장치다.

- 고르는 자리는 `09_요금제시작`이다. 체험 중 언제든 다시 들어와 바꿀 수 있고, 고른 것을 지울 수도 있다
- 고른 요금제는 **체험 중 기능 범위에 영향을 주지 않는다.** 무엇을 골랐든 전 기능이 열려 있고 문자 한도도 100건 그대로다(§2). 고른 값은 종료 시점에만 쓰인다
- **결제 수단은 이 단계에서 받지 않는다.** 카드는 실제로 결제하는 시점에 받는다(§2). 미리 고르기는 결제 예약이 아니다
- 종료 시 고른 것이 있으면 결제 화면이 그 요금제로 채워진 채 열리고, 없으면 고르는 단계부터 시작한다
- **유도 문구가 구체화된다.** 고른 것이 있으면 잔여일 안내와 종료 화면이 그 요금제와 금액을 그대로 말할 수 있다. §6의 유도가 일반 문구에서 자기 선택으로 바뀐다

## 6. 체험 종료 시 무엇이 멈추는가

이 서비스는 **발행이 수동**이다(`콘텐츠검토_정책.md` §1). 체험 중 만든 블로그 글과 문자는 이미 사장님이 채널에 올려 손에 있으므로, 체험이 끝나도 잃지 않는다. 따라서 "만든 것을 잃는다"는 압박은 이 서비스에 성립하지 않는다.

실제로 멈추는 것은 둘이고, **유도 문구는 이 둘에만 건다.**

| 멈추는 것 | 왜 손실인가 |
|---|---|
| **AI 학습 결과** | 매장 정보·자료·채널을 읽어 쌓은 것이 그 시점에 멈춘다. 다시 시작하면 처음부터 배운다 |
| **챗봇 응대** | 운영 중이던 챗봇이 응대를 멈춘다. 사장님이 아니라 **손님이 겪는다** |

만들어 둔 콘텐츠·자료·매장 정보 자체는 지우지 않는다. 결제하면 이어서 쓴다.

## 7. 전환 유도

### 7-1. 원칙

- **작업 흐름을 끊지 않는다.** 모달로 막지 않는다. 이 서비스는 오늘 할 일을 처리하는 곳이라 인터럽트가 특히 해롭다. 배너·뱃지·알림으로 한다
- **자기 매장의 실제 수치로 말한다.** 일반적인 효능 문구 대신 체험 중 쌓인 숫자를 인용한다(`08_실행결과리포트`가 이미 집계한다)
- **손실 문구는 §6의 둘로 한정한다**

### 7-2. 유도 자리

| 자리 | 화면 | 방법 |
|---|---|---|
| 계정 블록 | 전 화면 LNB 하단 | 요금제명 자리에 잔여일 표시 |
| 상단 띠 | `01_홈` | 잔여일 + 요금제 시작 진입. L3 전용 `uw-agency-strip`과 같은 위치 |
| 오늘 할 일 큐 | `01_홈` | 마지막 2일에만 항목 추가. 핵심 동선이라 강도가 높다 |
| 알림 | `18_알림센터` + 상단 벨 | D-3, D-1, D-0 세 번 |
| 기능 화면 | `15_AI학습현황`, `05_AI챗봇` | 그 기능이 멈춘다는 것을 그 화면에서 알린다 |
| 성과 | `08_실행결과리포트` | 체험 성과 요약을 유도 문구의 근거로 인용 |

### 7-3. 강도

| 시점 | 자리 | 성격 |
|---|---|---|
| 시작일 | 홈 띠 | 안내 |
| D-5 | 계정 블록만 | 무음 |
| D-3 | 알림 + 홈 띠 | 성과 인용 |
| D-1 | 알림 + 홈 띠 + 할 일 큐 | 손실 예고 |
| D-0 | 알림 + 홈 띠 | 손실 예고 |
| 종료 | `01_홈_미결제` | 잠금 + 시작 유도 |

문자 100건을 소진한 시점에도 한 번 알린다. 잔여일과 무관하다.

## 8. 대행사(L3) 쪽 처리

- `09_담당매장청구`의 KPI에 `체험 중`을 더한다. 지금은 `구독 중 / 만료 임박 / 연체 / 미개시` 4종이다
- 매장 목록에 잔여일을 뱃지로 표시해 종료가 가까운 매장을 대행사 직원이 먼저 챙길 수 있게 한다
- 대행사가 사장님 대신 결제하지는 않는다. L2 셀프의 구독은 사장님 본인 것이다

## 미해결 / 확정만 되고 미구현

- **화면 `09_요금제시작`이 아직 없다.** 요금제 미리 고르기와 바로 결제를 담아야 한다. Screen ID `UW_BL_08`은 가칭이고 매트릭스 등재 시 확정한다
- **`UW_MOCK`에 체험 관련 필드가 없다.** 지금은 `subStatus`와 `renewal`뿐이라 잔여일을 계산할 값이 없다. 체험 종료 시각과 미리 고른 요금제 필드가 필요하다
- **체험 종료 후 데이터 보관 기간이 미정.** §6에서 지우지 않는다고 했으나 무기한인지 정하지 않았다
- **체험 중 결제하면 남은 체험 기간을 어떻게 하는지 미정.** 즉시 유료로 전환하고 남은 일수를 버리는지, 첫 결제일을 체험 종료일로 미루는지 정해야 한다
- **문자 100건을 체험 중 결제로 넘어갈 때 어떻게 이어받는지 미정.** 체험 한도와 요금제 한도가 별개인지 합산인지

## 변경 이력

- **2026-09-04 신설**: L2 셀프의 최초 요금제 설정 경로가 없다는 것을 IA 점검 중 발견. 요금제 화면 셋이 모두 구독 중을 전제하고 있었고, `01_홈_미결제`는 "매장 정보 설정도 요금제를 시작한 뒤에 할 수 있다"고 안내하는데 실제 가입 동선은 회원가입에서 온보딩으로 곧장 넘어가 요금제를 지나치고 있었다. 화면 신설(가), 온보딩 뒤에 배치(B)로 방향을 정하고 그 앞에 7일 체험을 두기로 함. 카드 미등록 · 문자 100건 제한 · L2 전용은 같은 논의에서 확정
- **2026-09-04 체험 시작 시점을 가입 완료로 변경**: 처음엔 `09_요금제시작`에서 체험 시작 버튼을 누르는 안이었으나, 온보딩 중간 이탈 시 상태를 체험 중으로 두기로 하면서 시작 시점이 그보다 앞이어야 했다. 시작 버튼을 없애고 가입 완료에 붙이자 이탈 상태를 따로 정의할 필요가 사라졌고, `09_요금제시작`의 역할도 체험을 여는 화면에서 요금제를 정하는 화면으로 바뀌었다. 이에 따라 L2 사장님의 `미개시`는 체험 이전이 아니라 체험 이후 상태가 됨(§4)
- **2026-09-04 문자 100건을 발송 성공 기준으로 확정**: `09_요금제비용`의 사용량 집계가 성공 기준이라 그쪽에 맞췄다. 두 화면이 다른 답을 주지 않게 하기 위함
- **2026-09-04 요금제 미리 고르기 채택**: 체험 중 결제 없이 요금제만 골라 두게 함(§5). 결제 수단을 안 받는 정책과 충돌하지 않도록 미리 고르기를 결제 예약이 아니라 선택 저장으로 한정했고, 고른 값이 체험 중 기능 범위에 영향을 주지 않도록 못 박았다
