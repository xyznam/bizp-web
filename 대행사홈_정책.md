# 대행사 홈 페이지 정책

- 관련 화면: `17_대행사홈.html`(UW_HM_02), 연결: `12_매장목록.html`, `02_콘텐츠검토.html`, `15_AI학습현황.html`, `17_요청관리.html`, `09_요금제비용.html`
- 진입 경로: L3 LNB의 "🗂 대행사 홈"(L3 전용 그룹)

## 1. 접근 권한

L3(대행사) 전용. L1(위탁)·L2(셀프)는 안내 카드로 막힘: "이 화면은 대행사 직원 전용이에요, 대행사 홈은 담당 대행사 직원이 여러 매장의 오늘 할 일을 한눈에 보는 화면이에요" + [홈으로 가기].

## 2. KPI 타일 4종 (M01)

담당 매장(78곳, 고정 규모 지표) / 오늘 할 일(14건, M02·M03·M04 합산 총량) / 학습 실패(1곳) / 5일+ 방치(1건). 학습 실패·5일+ 방치 두 타일은 §4 요주의 매장의 두 조건과 대응된다.

## 3. 오늘 처리할 일 정렬 (M02)

**심각도 등급(tier)이 먼저, 등급 안에서는 오래 기다린 순.**

| 등급 | 의미 | 항목 유형 |
|---|---|---|
| 1등급 | 서비스 중단 — 방치하면 그 매장 전체가 멈춤 | 수집 실패·학습 실패 |
| 2등급 | 방치 위험 — 등급 안에서는 오래된 순으로 정렬 | N일+ 대기, 문의 답변 대기 |
| 3등급 | 정상 워크플로 — 방치돼도 서비스는 안 멈춤, 밀린 업무 | 검수 대기, 등록 대기 |

예시 순서(mock 기준): ① 카페봄봄 신촌점(수집 실패, 1등급) → ② 스시오마카세 판교점(5일째 대기, 2등급·더 오래됨) → ③ 청담라인의원(문의 답변 대기, 2등급·더 최근) → ④ 스시오마카세 강남점(검수 대기 3건, 3등급) → ⑤ 스시오마카세 강남점(등록 대기 1건, 3등급).

모든 행이 실제 링크(클릭 시 해당 처리 화면으로 이동). 하단 [할 일 전체 보기]는 14건 전량을 보되 지금은 `12_매장목록.html`로 연결(전용 "할 일 전체" 화면은 아직 없음).

## 4. 요주의 매장 정의 (M03)

**요주의 = 1등급(수집 실패·학습 실패) 또는 2등급 중 5일+ 방치, 둘 중 하나라도 해당하면 요주의.**

**제외**: 만료 임박(다음 결제일 근접), 결제 실패 — 위탁 매장은 대행사가 결제를 대신·일괄 처리할 확률이 높아 매장 단위 결제 리스크로 보기 애매하기 때문(자세한 근거는 §8).

**표시 방식**: 카드에는 가장 심각한 1곳(1등급 우선)만 예시로 노출하고, 제목 옆에 "N곳 중 1곳"이라고 명시해 나머지가 더 있음을 알 수 있게 한다. [매장 목록에서 보기]로 나머지 확인.

## 5. 제안 예정 (M04)

이 목록은 전부 **AI 초안 단계**(아직 검토·승인 전)다 — "AI가 이 시점까지 초안을 준비해 둘 예정"이라는 뜻이지 "이 시점에 나간다"는 뜻이 아니다.

- 각 항목에 보라색 'AI 초안' 배지 표시
- 시각 정보 없이 **날짜만** 표시("오늘", "토요일") — 검토를 반드시 거쳐야 해서 정확한 시각이 의미가 없음
- 캡션: "아직 검토 전이에요. 검수하고 승인해야 실제로 나가요"

## 6. 매장 스위처 (헤더)

검색 가능한 드롭다운(담당 매장 중 mock 6곳만 목록화 — 78곳 전량 아님, 화면 목적상 예시). 매장 선택 시 토스트 후 `06_우리매장정보.html`로 이동.

## 7. 미해결/확산 시 필요한 것

- §3·§4는 정책으로 확정했으나 HTML의 정렬은 여전히 하드코딩(실제 매장 수가 늘면 코드로 계산하는 로직 필요)
- §4에서 제외한 만료 임박·결제 실패를 대행사 관점에서 아예 안 보여줘도 되는지, 다른 화면(09_요금제비용 M06)에서만 다뤄도 충분한지는 재확인 여지 있음
- [할 일 전체 보기]가 전용 화면 없이 `12_매장목록.html`로만 연결됨 — 14건 전량을 실제로 볼 수 있는 화면은 아직 없음
- 매장 스위처로 바뀐 컨텍스트가 이동한 화면 이후까지 유지되는지 미검증(`매장목록_정책.md`의 동일 항목과 같은 미해결 사항)

## 8. 변경 이력

- **2026-08-27**: §3 정렬 정책 확정. "우선순위 순"이라는 라벨은 있었지만 실제로는 정의된 기준 없이 하드코딩된 순서였던 것을 발견해 3단계 tier 체계로 확정
- **2026-08-27**: §4 요주의 정의 확정. 처음엔 "만료 임박"도 포함하는 안을 검토했으나 제외로 정정 — 위탁 매장은 대행사가 결제를 대신하거나 일괄 처리할 확률이 높고, 모든 매장이 매달 한 번씩 결제 갱신일을 맞기 때문에 "만료 임박"만으로 요주의를 매기면 상시 다수 매장이 걸려 신호로서 의미가 없어짐. `09_요금제비용.html` M06(담당 매장 청구 현황, 대행사 통합 청구 관점)과도 같은 맥락. 결제 실패도 같은 이유로 일단 제외
- **2026-08-27**: §5 "발행 예정"을 "제안 예정"으로 개명. 기존 이름이 정해진 시각에 자동으로 발행된다는 뉘앙스를 줘서 `콘텐츠검토_정책.md`의 발행수동원칙(자동 발행 없음)과 어긋났던 것을 정정
- **2026-08-27**: §4 표시 방식에 카운트 표기 추가. 이전 리디자인 때(As-Is 카드 6장 중 5장이 죽은 링크였던 문제) 실링크 1장 + 목록 이동으로 단순화했는데, 그 결과 "요주의가 1곳뿐"처럼 보이는 부작용이 있어 이번에 "N곳 중 1곳" 표기로 보완
