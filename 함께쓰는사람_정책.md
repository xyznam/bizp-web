# 함께 쓰는 사람 페이지 정책

- 관련 화면: `20_함께쓰는사람.html`(UW_ST_07), 연결: `06_우리매장정보.html`(UW_ST_01), `17_직원관리.html`(대행사 직원판 — 역할 카드·초대 검증 로직 모두 여기가 원형, `직원관리_정책.md` 참조), `매장구조_회원정책.md` §3(역할 정의 원본)
- 이 화면은 **매장(소호) 직원**을 다룬다. `17_직원관리.html`은 **대행사 직원**을 다루는 별개 화면 — 이름이 비슷해 혼동하기 쉬우니 구분

## 1. 역할 — 사장님(조건부) / 매니저 / 운영자

`매장구조_회원정책.md` §3 정의를 그대로 쓴다: 매니저는 비용청구를 제외하고 대표와 같은 권한, 운영자는 직원관리를 제외하고 매니저와 같은 권한. 매니저/운영자 사이에서 실질적으로 갈리는 지점은 **직원관리(직원 초대·역할 편집) 접근 여부** 하나다 — 매니저는 가능, 운영자는 불가.

- 초대 모달·권한 정하기 패널 모두 역할 카드로 항상 펼쳐서 보여준다(select 아님, §4 참조)
- 초대 시 기본값은 **운영자**(최소 권한 우선)
- **실제 화면 접근 차단(운영자가 직원 모드로 이 화면에 들어오면 막는 것)은 방향만 확정하고 프로토타입에는 구현하지 않았다** — §7 미해결 참조. 지금은 역할이 배지·설명 문구로만 드러난다

**사장님(대표) 초대**

- **매장에 대표가 아직 없을 때만**(매니저만 있는 매장) 초대 모달의 역할 카드에 "사장님"이 추가로 노출된다. 대표는 매장당 1명이라 이미 있으면 이 옵션 자체가 사라진다
- 사장님으로 초대하면 편집·승인 권한 토글이 통째로 숨겨진다 — 대표는 항상 모든 권한을 가지므로 켜고 끌 대상이 아님
- 기존 직원의 역할을 나중에 "사장님"으로 바꾸는 건 안 된다(권한 정하기 패널의 역할 카드는 매니저/운영자만 제공) — 대표 위임은 셀프서비스 대상이 아니라 어드민 경유만 가능(`매장구조_회원정책.md` §5)
- 사장님이 합류하면 `12_매장목록.html`의 "사장님/매니저" 요약 행이 매니저에서 대표로 전환된다(`매장목록_정책.md` §5) — 매니저가 팀에서 빠지는 건 아니고, 대행사가 보는 한 줄 요약이 대표 우선으로 바뀔 뿐

## 2. 편집·승인 권한 — 단일 토글

문자·챗봇·콘텐츠 전 영역을 **"편집·승인 권한" 하나**로 통제한다 — 있으면 직접 편집·승인할 수 있고, 없으면 전부 조회만 가능하다.

- 역할과 별개 축이다. 매니저든 운영자든 편집·승인 권한은 따로 켜고 꺼야 한다(17_직원관리의 "승인 권한" 스위치와 동일한 자리)
- 초대 시 기본값은 **꺼짐**(읽기 전용)
- 켜짐 상태가 실제로 부여하는 것(`UW_PERMS.presets.edit_approve.grants`): AI 다시 학습시키기, 콘텐츠 만들기, 콘텐츠 승인·반려, 문자 만들기·수정, 문자 발송 승인, 챗봇 응대 설정 — 6개
- **콘텐츠/문자/챗봇 조회는 권한과 무관하게 항상 가능하다**(baseline)
- 손님 응대(AI챗봇 실시간 채팅)도 이 권한 체계 밖에서 전 직원 기본 허용

## 3. 화면별 실제 효과 (레벨별)

L1(위탁)은 콘텐츠 만들기·문자 만들기·재학습 기능 자체가 이 매장에 없다(대행사가 처리) — 그래서 L1 매장 직원은 편집·승인 권한을 받아도 "승인"만 할 수 있다.

| 화면 | 레벨 | 권한 없음(읽기만) | 편집·승인 권한 있음 |
|---|---|---|---|
| 02_콘텐츠검토 | L1 | 목록 조회만 | 승인·반려만(문구 수정·AI 다시쓰기는 이 레벨에 없음) |
| | L2·L3 | 목록 조회만 | 승인·반려 + 문구 직접수정·AI 다시쓰기 |
| 02_발행이력 | 전 레벨 | 이력 조회(원래 조회 전용 화면) | 동일 |
| 03_콘텐츠만들기 | L1 | 위저드 안내만 | 동일(이 레벨엔 생성 기능 자체가 없음) |
| | L2·L3 | 위저드 안내만 | 콘텐츠 생성(스텝1~3, 검토요청·수정) |
| 04_비즈챗문자 | L1 | 발송 현황 조회만 | 승인하고 예약만(문자 만들기·수정은 이 레벨에 없음) |
| | L2·L3 | 발송 현황 조회만 | 승인+예약 / 새 문자 만들기·수정 |
| 05_AI챗봇 | 전 레벨 | KPI·상담내역 조회. 손님 응대는 권한 무관 항상 가능 | 위 + 챗봇 수정 요청하기 |
| 15_AI학습현황 | L1 | 학습 현황 조회만 | 동일(이 레벨엔 재학습 기능 자체가 없음) |
| | L2·L3 | 학습 현황 조회만 | 다시 학습하기 |

## 4. 초대·편집 UI — select 대신 항상 펼쳐진 카드

`소상공인 마케팅 웹앱 디자인 가이드` §7 "Select · Combobox · DropdownMenu → 라디오 카드 유지(금지)" 규칙에 따라, 역할은 드롭다운이 아니라 **선택지 2개를 항상 펼쳐 보여주는 카드**로 구현한다(카드 안에 "직원 초대·관리 가능/불가" 한 줄 설명 병기). 편집·승인 권한은 별도 스위치 1개.

- 이 규칙은 초대 모달·권한 정하기 패널(M03) 양쪽에 동일하게 적용
- `17_직원관리.html`도 같은 규칙으로 동일한 카드형 UI를 쓴다

## 5. 직원 초대 검증 (이름+휴대폰, 4케이스)

`17_직원관리.html`과 동일한 검증 로직을 쓴다(대행사 직원 초대와 정책이 같음).

| 케이스 | 처리 |
|---|---|
| 이미 우리 매장 직원(초대 대기 포함)인 번호 | 차단, 이름을 특정해 안내("OO님은 이미 우리 매장 직원이에요") |
| 그 외 이미 가입된 계정 + 이름 일치 | 소속수락 문자 발송, 정상 진행(상대방은 비즈플래닛 계정은 있지만 우리 매장 직원은 아닌 경우) |
| 그 외 이미 가입된 계정 + 이름 불일치 | 차단, 상대방 실명은 노출하지 않고 일반화된 안내만("이름과 번호가 맞는지 다시 확인해 주세요") |
| 신규 번호 | 정상 진행 |

- 이름·번호는 입력을 고치면 차단 안내가 즉시 사라진다(재시도 유도)
- 정상 진행(2·4번 케이스)돼도 직원 목록에 실제로 추가되지는 않는다 — 토스트만 뜨는 시뮬레이션(§7 미해결)

## 6. 직원 해지 — 완전 삭제 (이용 중지 아님)

이미 합류한 직원을 내보내는 기능. `17_직원관리.html`과 동일하게 **완전 삭제**로 처리한다 — 목록에서 즉시 사라지고 되돌릴 수 없다.

- 위치: 권한 정하기 패널(M03) 하단, [권한 저장] 옆에 [직원 해지] 버튼
- 확인 모달에서 이름을 특정해 안내("OO님은 더 이상 이 매장에 접근할 수 없어요") → 확정 시 목록·상세 패널 모두 즉시 반영
- **"이용 중지" 상태 토글이 아니라 완전 삭제인 이유**: 이 화면엔 17의 "배정 변경 이력" 같은 활동 로그가 없어 되돌리기용 기록을 남겨야 할 이유가 약하고, 대행사 화면(17)과 같은 방식을 써야 두 화면을 같이 쓰는 사람이 헷갈리지 않는다

## 7. 미해결 / 미구현

- **역할 기반 화면 접근 차단 미구현** — "운영자는 이 화면(함께 쓰는 사람) 자체에 못 들어온다"는 방향은 확정했지만, 이 프로토타입엔 직원이 실제로 로그인해서 보는 흐름이 없어 강제할 방법이 없다. 실제 구현은 백엔드 인증이 붙는 시점으로 미룸
- **초대해도 직원 목록에 실제로 추가되지 않음** — `17_직원관리`는 초대 시 목록에 "초대 대기" 상태로 실제 추가되는데, 이 화면은 초대 모달이 토스트만 띄우고 끝나는 얕은 시뮬레이션이다. 검증 로직(§5)은 있지만 성공 후 목록 반영은 안 됨
- **M05 "보낸 초대"는 여전히 정적 예시 하나뿐** — 이수진(대기 1건) 카드가 배열이 아니라 하드코딩. §5 검증 로직에서 매칭용으로만 참조(`PENDING_INVITE`), 실제 목록 렌더링과는 분리돼 있음

## 변경 이력

- **권한 모델을 프리셋 3종+개별 토글 8개 → 편집·승인 권한 단일 토글로 축소(2026-09-02)**: 기존엔 문자·챗봇·콘텐츠 영역별로 세부 기능을 하나씩 켜고 끌 수 있었으나, "권한 있으면 다 되고 없으면 읽기만"이라는 더 단순한 규칙으로 통일. 이 과정에서 `content.view`가 개별 권한 항목이었던 게 새 규칙과 모순됨을 발견해 baseline(항상 조회 가능)으로 옮겼고, 실제로 연결된 화면이 없던 죽은 항목 `bot.material`도 같이 삭제
- **초대 시 권한 선택을 역할(매니저/운영자)로 전환**: 대행사 직원판(`17_직원관리`)과 동일한 개념(역할)을 쓰도록 통일. 다만 역할의 의미(직원관리 접근 가부)는 아직 화면 차단으로 실제 구현되지 않음(§6)
- **select → 라디오 카드로 변경**: 처음엔 17을 그대로 본떠 `<select>`로 만들려 했으나, 디자인 가이드 §7이 드롭다운을 금지하고 있음을 확인해 카드형으로 바꿈(§4) — 17 쪽은 같은 문제가 있지만 이번 스코프에서는 남겨둠
- **mock 직원 이름 "김하나 매니저" → "김하나"로 정리**: 직함을 이름에 섞지 않는다는 원칙(대행사 직원 `17_직원관리`에서 먼저 확정)을 이 화면에도 동일 적용 — 역할은 이름 옆 배지로만 표시
- **"이 직원 화면 미리보기"(M04) 삭제(2026-09-02)**: 이 화면(20)이 `ownerOnly`라서, 미리보기로 role을 staff로 바꾸는 순간 같은 페이지에서 곧바로 자기 자신의 "사장님만 볼 수 있는 화면이에요" 잠금에 걸려 제대로 동작하지 않았음(재렌더링 시 자기 자신을 잠그는 구조적 문제). 오버스펙 판단과 함께 제거 — capability-matrix.js의 M04 모듈 등록도 같이 삭제
- **`17_직원관리.html`의 역할 select도 라디오 카드로 통일(2026-09-02)**: §4에서 확인된 디자인 가이드 §7 위반이 17에도 동일하게 있어 함께 정리. 이 김에 17의 초대 검증 로직(4케이스)을 이 화면에도 이식(§5) — 이전엔 이름·번호 검증 자체가 없었음
- **사장님(대표) 초대 옵션 추가(2026-09-02)**: 대표가 없는 매장(매니저만 있음)을 계속 그 상태로 방치할 수밖에 없었던 걸 발견해 추가. 특히 대행사 없는 L2 셀프 매장은 `12_매장목록.html`(L3 전용) 자체에 접근할 수 있는 사람이 없어, 이 화면이 대표를 들일 수 있는 유일한 경로였음. `매장목록_정책.md` §5에 "함께쓰는 사람 메뉴 예정"으로 이미 예고돼 있던 기능
- **직원 해지 기능 신설(2026-09-02)**: 이미 합류한 직원을 내보낼 방법이 이 화면에 아예 없던 걸 발견해 추가. 처음엔 M05 캡션 문구("이용 중지로 바꿔주세요")를 근거로 상태 토글(비활성화, 기록 유지)을 검토했으나, 17과의 일관성·이 화면에 되돌리기용 기록(활동 로그)이 없다는 점을 이유로 17과 동일한 완전 삭제 방식으로 확정(§6). M05 캡션도 실제 기능에 맞게 수정
