# 마케팅 계획 페이지 정책

- 관련 화면: `07_마케팅계획.html`(UW_PL_01), 연결: `03_콘텐츠만들기.html`, `04_비즈챗문자.html`, `15_AI학습현황.html`

## 1. 월 계획 자동 생성 배너 (M01)

매월 셋째주 월요일에 다음 달 계획(날짜+채널 슬롯만, 구체적 주제는 미정)이 자동 생성되고 전원(L1/L2/L3 모두)에게 알림만 간다. 승인 절차·대행사 검수 단계는 없다 — 대행사도 사장님과 동일하게 조정 권한만 가진다. 아무도 조정하지 않으면 생성된 대로 그대로 진행된다("확인" 버튼 자체가 없음).

L1은 배너 안내 문구가 "대행사가 대신 관리해요"로 바뀌고, §2 조정하기 버튼 대신 "대행사에 요청하기" fallback 카드로 자동 대체된다.

## 2. 조정하기 모드 (M05, L2+L3)

버튼을 누르면 조정 모드가 켜지고 캘린더 칩마다 × 버튼(빼기)이 붙는다. 칩을 클릭하면 "옮길 날짜 칸을 눌러주세요" 안내 후 다른 날짜 칸을 클릭해 이동시킨다. 빼기/옮기기 모두 즉시 화면에 반영되고 토스트로 안내한다.

L1은 이 버튼 자체가 §1의 fallback 카드로 대체되어 노출되지 않는다.

## 3. 다른 매장에도 적용 (M07, L3 전용)

이 계획을 담당 다른 매장에도 일괄 적용하는 스트립. 클릭 시 토스트만 뜨는 시뮬레이션.

## 4. 월 캘린더 (M02)

- 채널 dot 범례: 블로그(파랑) / 문자(초록) / 주제 미정(점선 원)
- 발행 TOPIC_LEAD_DAYS(기본 3)일 전까지는 "OO 예정"으로만 표시하고, 그 안에 들어오면 구체적 주제 제목이 노출된다
- 칩을 클릭하면(조정 모드가 아닐 때) §5 일정 미리보기 모달이 열린다
- 그리드 아래 아젠다 리스트가 같은 데이터를 리스트 형태로 병행 노출(모바일 폭에서는 그리드 칩이 색 점으로 축소되고 아젠다 리스트가 상세 정보를 담당)

## 5. 일정 미리보기 (M03)

조회 전용, 닫기만 가능. 주제 미확정 상태에서는 "주제는 발행 N일 전에 AI가 정해요" 안내로 대체된다.

## 6. 제안 주기 / AI 학습 주기 요약

캘린더 카드 안, 월 이동 줄 바로 아래·그리드 위에 텍스트 두 줄로 노출한다. 배경 박스 없이 라벨+값+버튼만 있는 간결한 행이며(처음엔 회색 배경 박스를 씌운 카드형으로 만들었으나 화면이 무거워진다는 이유로 박스를 제거함, §변경 이력), 각 행 끝의 버튼은 같은 크기(`btn-ghost btn-sm`)로 나란히 배치한다.

| 행 | 값 | 버튼 | 노출 레벨 | 편집 위치 |
|---|---|---|---|---|
| 제안 주기 | 주당 블로그 횟수 · 문자 월 제안 횟수 | ⚙ 제안 주기(기어 아이콘) → §7 모달 | L2+L3 (L1은 행 자체 숨김) | 이 화면(§7) |
| AI 학습 주기 | 블로그 · 플레이스 리뷰 · 매장정보, 카테고리별 학습 주기 | AI학습현황(아이콘 없음) → `15_AI학습현황.html` 이동 | 전 레벨(L1 포함) | `15_AI학습현황.html` 수집 설정 모달(정본, L3 전용 편집) |

두 버튼의 아이콘 유무 차이는 의도된 것이다 — 기어 아이콘이 있는 쪽은 "이 화면에서 바로 편집", 없는 쪽은 "다른 화면으로 가서 확인/편집"임을 같은 크기·스타일 안에서 구분하기 위함.

AI 학습 주기 행이 L1까지 포함해 전 레벨 노출인 이유: 이 값은 "언제 새 글 주제가 반영되는지"를 이해하는 배경 정보라서, 대행사가 관리하더라도 사장님이 참고할 필요가 있다고 판단(§1의 "대행사가 관리하는 값은 L1에 숨김" 원칙과는 다른 성격 — 학습 주기는 관리 권한이 아니라 조회용 배경 정보).

## 7. 제안 주기 설정 모달 (M08)

- 주당 블로그 횟수(1/2/3회), 문자 월 제안 횟수(1/2/3회)를 라디오로 선택
- 문자 옆 캡션: "여기서 정하는 건 '몇 번 제안할지'예요. 실제 발송은 매번 검토·승인을 거쳐요" — 이 설정이 발송 자동화가 아님을 명시
- 저장 시 토스트("제안 주기를 저장했어요. 다음 달 계획부터 반영돼요")만 뜨고 실제 계획 생성 로직에는 반영되지 않음(미해결 참조)

## 8. 일정 직접 추가 없음

이 화면에 "일정 추가" 기능은 없다(2026-08-26 결정). 직접 만들고 싶은 콘텐츠가 있으면 `03_콘텐츠만들기.html` 또는 `04_비즈챗문자.html`로 바로 이동해서 만들면 되므로, 이 화면은 자동 생성된 계획을 보고 조정(빼기/옮기기)만 하는 역할로 한정한다.

## 미해결 / 미구현

- §7 제안 주기 저장이 실제로 §4 캘린더 생성 로직에 반영되지 않음(토스트만)
- §2 조정하기로 뺀/옮긴 일정은 새로고침하면 초기 mock으로 되돌아감(영속화 없음)
- §3 다매장 일괄 적용도 토스트만, 다른 매장 데이터에 실제로 반영되지 않음

## 변경 이력

- **정책 문서 신설(2026-09-02)**: 그동안 이 화면 전용 정책 문서가 없었음. §6 제안 주기/AI 학습 주기 요약 신설을 계기로 화면 전체 정책을 정리해 새로 작성
- **§6 제안 주기 / AI 학습 주기 요약 신설(2026-09-02)**: 기존에는 "⚙ 계획 룰" 버튼 하나만 월 이동 줄 안에 있었음. `15_AI학습현황.html`의 학습 주기가 블로그/리뷰/매장정보 카테고리별 값으로 나뉘면서(AI학습현황_정책.md §6) 이 화면에서도 학습 주기를 참고할 수 있어야 한다는 논의로 요약 행을 추가하기로 함. 처음엔 두 행을 회색 배경 박스(카드)로 감싸 만들었는데 화면이 무거워진다는 피드백을 받아 박스를 제거하고, 텍스트 + 같은 스타일 버튼을 나란히 배치하는 지금 형태로 재조정했다. 이 과정에서 모달 안 "채널 비중은 매장 상황에 따라 AI가 자동으로 조정해요"라는 문구/개념도 무엇을 뜻하는지 불명확하다는 이유로 함께 제거함
- **"⚙ 계획 룰" → "⚙ 제안 주기" 개명(2026-09-02)**: "계획 룰"이라는 이름이 실제 기능(제안 횟수를 정하는 것)을 드러내지 못한다는 이유로 이름을 바꿈
