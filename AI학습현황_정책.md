# AI 학습현황 페이지 정책

- 관련 화면: `15_AI학습현황.html`(UW_ST_05), 연결: `06_우리매장정보.html`(UW_ST_01), `99_매장자료관리.html`

## 1. 학습 재료 요약 (M09)

이 화면 상단에서 AI가 무엇을 읽고 학습하는지 3개 타일로 요약한다: 🏪 매장정보 / 📁 매장자료 / 🔗 채널 콘텐츠. 각 타일은 상태 서브라벨(예: "입력 완료", "4개 등록됨", "3개 채널 연결됨")과 함께 원본 관리 화면으로 이동하는 링크를 갖는다(매장정보→`06_우리매장정보.html`, 매장자료→`99_매장자료관리.html`, 채널 콘텐츠→`06_우리매장정보.html#channel-manage`).

**L2·L3에만 노출, L1은 숨김**(fallback 카드 없이 단순 숨김). L1은 채널·학습 자체를 대행사가 관리하는 레벨(§7)이라 "무엇을 학습 재료로 쓰는지"를 사장님이 직접 관리할 필요가 없다고 판단. 다른 `type:"view"` 모듈들은 보통 L1도 포함해 전 레벨에 노출하는 게 이 프로젝트의 기본 관례인데, M09는 그 관례의 첫 예외 케이스.

## 2. AI 학습 현황 배너 (M02)

상태 기반 단일 뷰 — 아래 4가지 상태 중 하나를 배너로 보여준다. 프로토타입은 우측 상단의 "데모: 상태 전환" 스위치로 4개 상태를 직접 토글해 확인할 수 있다(실제 서비스에서는 이 스위치 없이 상태값에 따라 자동 결정).

| 상태 | 의미 | 진행 바 |
|---|---|---|
| 정상(`ok`) | 최근 학습 정상 완료, 다음 학습 예정일 안내 | 없음 |
| 수집 실패(`collect-fail`) | 채널 주소가 바뀌는 등의 이유로 글을 못 가져옴 | 없음 |
| 분석 실패(`analyze-fail`) | 수집은 됐지만 정리(분석) 단계에서 문제 발생 | 없음 |
| 재학습 중(`retraining`) | 학습 진행 중 | 있음(진행률 표시) |

**재학습 중에는 [다시 학습하기] 버튼이 비활성화된다** — 중복 실행 방지.

## 3. 다시 학습하기 (M04)

L2·L3만 실행 가능(`fallback: agency-card`로 L1은 "대행사에 요청" 카드로 자동 대체). 실행 시 토스트로 "학습을 시작했어요. 약 3분 걸려요"만 안내하는 시뮬레이션이며, 실제로 §2 배너 상태를 `retraining`으로 전환하지는 않는다(프로토타입 한계 — 미해결 참조).

## 4. AI가 읽은 콘텐츠 (M03)

블로그 글 / 손님 리뷰 / 매장 자료, 3개 탭으로 구성. 탭마다 표시 방식이 다르다.

- **블로그·리뷰 탭**: L3에서만 각 항목 앞에 체크박스가 붙어 개별 포함/제외 선택 가능(L1·L2는 목록만 조회). "AI가 쓴 것 같은 글"로 판정된 항목은 기본 체크 해제 상태로 시작하고 주황 배지가 붙는다.
- **매장 자료 탭**: 체크박스 없음(선택 개념이 없음, 등록된 자료는 전부 학습 대상). 안내 문구도 "마지막 학습 때 반영된 자료만 보여드려요"로 별도.
- 탭 전환 시 하단 보조 영역도 함께 바뀐다: 블로그·리뷰 탭은 §5의 [선택한 글만 배우게 하기] 버튼, 매장 자료 탭은 `99_매장자료관리.html`로 가는 [자료 추가·관리 →] 링크.

## 5. 학습 콘텐츠 선택 저장 (M06)

L3 전용, 블로그·리뷰 탭에서만 노출(§4). [선택한 글만 배우게 하기] 클릭 시 토스트 "선택을 저장했어요. 다음 학습부터 반영돼요"만 실행하는 시뮬레이션 — 실제로 체크 상태를 저장하거나 §4 목록에 반영하지는 않는다.

## 6. 수집 설정 (M07)

L3 전용, ⚙ 아이콘 버튼으로 모달 진입. §1의 학습 재료 3종 중 **수집 대상 2종(블로그·플레이스 리뷰)** + **매장정보**, 총 3개 콘텐츠 카테고리별로 범위·주기를 나눠 설정한다(매장 자료는 사장님이 직접 업로드하는 파일이라 수집 개념이 아니므로 대상 아님).

**"수집 범위"와 "학습 주기"는 같은 축이 아니다** — 수집 범위는 **최초 연결(또는 설정 변경) 시 과거를 얼마나 소급해서 가져올지**를 정하고, 학습 주기는 **그 이후 새 콘텐츠를 얼마나 자주 확인할지**를 정한다. 이미 가져온 기간을 학습 주기마다 매번 다시 훑는 게 아니다 — "매주 학습인데 범위가 3개월이면 매주 3개월치를 중복 수집하는 거 아니냐"는 질문에서 이 구분이 명확해짐(모달 상단 안내 문구로도 노출).

| 카테고리 | 수집 범위(최초 소급) | 학습 주기 | 기본값 |
|---|---|---|---|
| 블로그 | 최근 3개월 / 6개월 / 전체 | 매주 / 2주마다 / 매달 | 전체 · 2주마다 |
| 플레이스 리뷰 | 최근 3개월 / 6개월 / 전체 | 매주 / 2주마다 / 매달 | 최근 3개월 · 2주마다 |
| 매장정보 | 없음(수집 범위 컨트롤 자체가 없음) | 매주 / 2주마다 / 매달 | 매주 |

- **매장정보만 수집 범위가 없는 이유**: 블로그·리뷰는 날짜순으로 계속 쌓이는 콘텐츠라 "얼마나 과거까지"가 의미 있지만, 매장정보(플레이스에 등록된 운영시간·메뉴·주소 등 사업정보)는 시계열이 아니라 **현재값 하나만 있는 고정 항목**이라 소급 개념 자체가 성립하지 않음. 항상 최신값 기준으로 확인하고, 학습 주기만 "얼마나 자주 재확인할지"로 설정
- **블로그가 리뷰보다 소급 범위 기본값이 넓은 이유**: 블로그는 사장님이 직접 쓴 글이라 오래된 글도 말투/브랜드 학습에 여전히 유효. 리뷰는 오래될수록 지금 가게 상태와 안 맞을 수 있어 최근 것 위주가 더 유용하다고 판단
- "담당 매장 전체에 같은 설정 적용" 체크박스(다매장 일괄 적용)는 카테고리 구분과 무관하게 공통으로 하단에 유지
- 저장 시 토스트만 실행(값 반영 없음, 미해결 참조)

## 7. 레벨별 안내 문구 차이

상단 서브타이틀(`topbar.sub`)이 레벨별로 역할 인식을 다르게 안내한다: L1은 "채널과 학습은 대행사가 관리해요 — 현황을 보여드려요"(조회 전용 인지), L2는 "AI가 우리 가게를 계속 배우고 있어요"(진행 중 인지), L3는 "채널 상태를 점검하고, 필요하면 다시 학습시키세요"(관리 책임 인지). §3·§5·§6의 레벨별 실행 권한 차이와 일관된 방향.

## 미해결 / 미구현

- **§3 "다시 학습하기"가 §2 배너 상태를 실제로 바꾸지 않음** — 토스트만 뜨고 배너는 그대로 "정상"에 머무름(배너 전환은 데모 스위치로만 가능)
- **§5 "선택한 글만 배우게 하기"가 §4 체크 상태를 저장/반영하지 않음** — 토스트만 뜨는 시뮬레이션
- **§6 수집 설정 값이 어디에도 반영되지 않음** — 저장해도 §2·§4에 아무 영향 없음(범위를 "최근 3개월"로 바꿔도 §4 콘텐츠 목록 개수 등은 그대로)

## 변경 이력

- **M09(학습 재료 요약) 화면 미노출 버그 발견 및 수정(2026-08-31)**: `capability-matrix.js`의 `UW_ST_05`에 M01~M07까지만 등록돼 있고 M09가 빠져 있어, `render()`의 미등록 모듈 자동 숨김 로직에 의해 화면 상단 3타일 섹션 전체가 항상 숨겨져 있던 실제 버그였음. 매트릭스에 M09를 `type:"view", levels:["L1","L2","L3"]`로 등록해 수정, 3개 레벨 모두에서 정상 노출 확인
- **정책 문서 신설**: 그동안 `15_AI학습현황.html`에 별도 정책 문서가 없었음(`우리매장정보_정책.md`에서 진입 카드로만 언급). M09 버그 수정을 계기로 이 화면 전용 정책 문서를 새로 작성
- **죽은 매트릭스 항목 M01·M05 삭제(2026-09-01)**: `capability-matrix.js`의 `UW_ST_05`에 등록돼 있던 M01("채널 연결 상태")·M05("채널 주소 관리")는 `15_AI학습현황.html`에 대응하는 `data-module` 요소가 실제로 없는 죽은 항목이었음(동일 기능은 이미 `UW_ST_01`의 M05로 정식 등록돼 있어 중복이었던 것으로 확인) — 삭제
- **M09 학습 재료 요약, L1 노출 대상에서 제외(2026-09-01)**: 처음 버그를 고칠 때는 임시로 전 레벨(L1/L2/L3) 노출로 등록했으나, L1은 학습·채널 자체를 대행사가 관리하는 레벨이라 사장님이 "무엇을 학습 재료로 쓰는지" 볼 필요가 없다고 판단해 L2·L3 전용으로 좁힘
- **§6 수집 설정, 화면 전체 단일 설정 → 콘텐츠 카테고리별 설정으로 전환(2026-09-01)**: 처음엔 "블로그·리뷰 통틀어 범위 하나 + 주기 하나"였는데, 콘텐츠 종류마다 성격이 다르다는 지적으로 블로그/플레이스 리뷰/매장정보 3개 카테고리로 분리. 분리 과정에서 "매주 학습인데 범위가 3개월이면 매번 중복 수집 아니냐"는 질문이 나와 수집 범위(최초 소급)와 학습 주기(이후 확인 빈도)가 서로 다른 축임을 명확히 하는 방향으로 정리했고, 매장정보는 소급 개념이 없는 고정 항목이라 수집 범위 컨트롤 자체를 뺐음
