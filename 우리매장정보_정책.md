# 우리 매장 정보 페이지 정책

- 관련 화면: `06_우리매장정보.html`(UW_ST_01), 연결: `06_우리매장정보_수정.html`(UW_ST_10), `98_매장설정.html`, `99_매장자료관리.html`, `16_마케팅전략상세.html`, `15_AI학습현황.html`, `02_콘텐츠검토.html`

## 1. 매장 상태 3단계

`매장구조_회원정책.md`의 콘텐츠 상태(설정전/학습전/운영중)가 이 화면에서 실제로 갈리는 지점.

- **설정전**: L1만 안내 카드("매장 정보를 아직 채우지 않았어요, 대행사에 요청하기") 노출. L2·L3는 이 화면 대신 `98_매장설정.html`로 리다이렉트
- **학습전**: 화면은 다 보이지만 `data-ob-lock` 표시된 영역(마케팅 전략 상세 등)이 잠금 오버레이("🔒 AI 학습이 끝나면 열려요")로 덮임
- **운영중**: 정상 상태, 전체 기능 노출

## 2. AI 이해도 게이지

"AI가 우리 가게를 OO% 이해했어요" 게이지의 배점은 다음과 같다.

**전제**: 학습전(`prelearn`) 상태는 이 배점표 대상이 아니다. AI 학습을 한 번도 안 한 상태는 %가 아니라 "학습을 시작하세요"라는 안내 배너에 가까우므로 낮은 고정값(현재 6%)으로 별도 취급한다. 아래 배점표는 **학습을 최소 1회 완료한 이후**부터 적용된다.

| 항목 | 배점 | 데이터 출처 |
|---|---|---|
| AI 학습 완료(최소 1회) | **50%** | `15_AI학습현황`의 학습 상태(운영중 여부) |
| 채널 연결 개수 | 채널당 **+10%p**(최대 3개: 네이버 블로그/네이버 플레이스/당근) | 이 화면의 "연결된 채널" 목록(`connected: true` 개수) |
| 매장 자료 등록 | 사진·메뉴판·소개문서 각 **+5%p**(최대 +15%p) | `99_매장자료관리`의 등록 여부 |
| 우리 가게 스타일 확인 | **+5%p** | `16_마케팅전략상세`에서 AI가 채운 기본값을 사장님이 한 번이라도 확인/수정 |

최대 100% = 50 + 30(채널 3개) + 15(자료 3종) + 5(스타일 확인).

**설계 의도**: 학습 완료가 가장 큰 비중(50%)인 이유는 이게 없으면 AI가 참고할 원본 데이터 자체가 없어서다. 게이지가 쉽게 100%를 채우지 않도록 설계했는데, 이는 화면의 CTA가 항상 의미를 갖게 하려는 목적이다.

**100% 도달 시**: 배점상 100%는 나올 수 있는 값이라(§2 상단 표대로 다 채우면 도달) 화면도 별도로 반응한다 — 막대 색이 주황(진행 중)에서 초록(완료)으로 바뀌고, 타이틀·캡션이 "다 채웠다"는 완료형 문구로 바뀌며, CTA도 `+ 정보 더 채우기`(뭔가 빠졌다는 뉘앙스)에서 `정보 관리하기`로 바뀐다. 목적지(99_매장자료관리)는 그대로 — 100%를 찍었다고 더 손댈 게 없는 건 아니고, §6 자료 최신성 관리는 계속 필요하기 때문이다.

**AI 학습현황 진입**: 게이지 배너 우측에 `+ 정보 더 채우기`(또는 100%일 땐 `정보 관리하기`)와 나란히 `AI 학습현황 →` 진입점이 있고, 그 아래 작은 글씨로 마지막 학습일이 붙는다(`15_AI학습현황` mock과 동일 문구 사용). **버튼이 아니라 파란 텍스트 링크**다 — 진한 배경의 주 액션(정보 더 채우기)과 나란히 두면 버튼끼리 위계가 부딪혀서, "연결된 채널" 카드의 `관리 →`와 같은 텍스트 링크 스타일로 통일했다. 학습전(prelearn) 상태는 학습을 한 번도 완료한 적이 없어 마지막 학습일 자체가 성립하지 않으므로 이 줄은 숨긴다. 예전엔 우측 사이드바에 별도 카드(§6)로 있었는데, 이 배너 안으로 합쳐졌다.

## 3. 매장 정보 수정 (기본 정보 / 영업시간 / 대표메뉴 / 추가정보)

전용 화면 이동 방식(별도 페이지 확정) — [매장 정보 수정하기] 클릭 시 `06_우리매장정보_수정.html`(UW_ST_10)로 이동. `24_해지.html`과 같은 "별도 파일 + 브레드크럼 복귀" 패턴(LNB 메뉴 항목 없음, 이 화면에서만 진입). UI는 `98_매장설정.html` Step2(정보 확인/입력)와 통일. 프로토타입에서는 저장해도 실제 반영 없이 원 화면(`06_우리매장정보.html`)으로 돌아가기만 함.

- **편집 가능 항목**: 업종/이름, 연락처, 주소, 영업시간(쉬는시간·라스트오더·요일별 설정 포함), 쉬는날, 주차, 대표 메뉴, 매장소개
- **사업자번호·이메일은 편집 항목에서 제외**: 98의 신규 등록 흐름에서도 함께 제거함(가입 시 이미 확보된 정보라 매장 정보 수정에서 다시 받을 필요 없다고 판단)
- **대표 메뉴는 칩(chip) 입력 방식**: 이름/가격을 각각 입력 후 [추가]하면 칩으로 쌓이고 개별 삭제 가능. 98의 신규 등록 Step2 대표메뉴 섹션도 동일한 칩 UI로 통일(다만 98은 신규 매장이라 빈 배열로 시작, 수정 화면은 기존 등록값으로 미리 채워짐). **업종별 프리셋에 따라 항목 자체가 달라져야 한다는 방향은 확정했으나 프리셋 설계는 추후 별도 진행**(§ 미해결 참조)
- L3 전용으로 "담당 매장 정보 일괄 관리"(여러 매장 영업시간·휴무일을 한 번에 변경) 별도 존재.

## 4. 연결된 채널

블로그/플레이스/당근/인스타그램 4종 중 연결된 것만 목록에 노출. 인스타그램은 OAuth 연동 시뮬레이션 모달(권한 요청 → 허용하고 연결)이 있고, 나머지 3종은 URL 직접 입력 방식. "채널 주소 관리" 모달에서 주소가 바뀌면 AI가 글을 못 가져온다는 경고 문구 있음 — 채널 연결 자체가 AI 학습의 원천 데이터이므로 §2 게이지 배점(채널당 10%p)과 직결됨.

## 5. 콘텐츠 검수·발행 방식 (모드 A/B)

`콘텐츠검토_정책.md`의 검수 모드 설정이 실제로 사는 위치가 이 화면이다.

- **레벨별 편집 권한**: L3(대행사)만 라디오를 실제로 바꿀 수 있음 / L1(위탁)은 라디오가 보이지만 비활성(읽기전용, "대행사에 요청해 주세요" 안내) / L2(셀프)는 옵션 자체를 숨기고 항상 모드A로 고정("대행사 위임은 대행사와 함께 쓸 때 선택 가능"이라는 캡션만 노출)
- 변경 시 `02_콘텐츠검토`에 즉시 반영된다는 전제(같은 `window.UW.reviewMode()` 값을 두 화면이 공유)

**16_마케팅전략상세와의 관계**: `16_마케팅전략상세.html`(L3, 이미지 스타일 탭)의 "검수 강도" 필드는 업종에 따라 강제되는 고정값이다(예: 의료 업종은 "생성 이미지는 발행 전 사장님 승인이 필요해요", 강화는 자유지만 완화는 막혀 있음). **이 필드가 제약 조건의 원천이고, 이 화면의 모드 A/B 설정이 그 제약을 따라야 한다** — 검수 강도가 "사장님 승인 필수"를 나타내는 업종에서는 모드B(대행사 검수 후 발행, 사장님 승인 생략)를 선택 불가 처리하고 모드A(사장님 검수 후 발행)만 고를 수 있게 한다. 검수 필수가 아닌 업종은 지금처럼 A/B 둘 다 선택 가능. 상세는 `마케팅전략상세_정책.md` §6 참조.

## 6. 매장 자료 / 마케팅 전략 상세 / 우리 가게 스타일

전부 다른 화면으로의 진입 카드 형태로, 이 화면 자체엔 상세 UI가 없다(AI 학습 현황 진입은 더 이상 별도 카드가 아니라 §2의 게이지 배너 안에 있다):
- **매장 자료**: 썸네일 3칸 미리보기 + 건수 요약("사진 O장 · 메뉴판 O개 · 소개문서 O개") + 최근 등록일과 "최신 자료가 있으면 학습에 더 도움돼요" 안내 + [자료 관리하기 →]는 `99_매장자료관리`로 이동. 예전엔 "추가하면 +N%" 배지가 있었는데 배점표와도 안 맞고 이미 3종을 다 등록한 mock 기준으로는 성립하지 않는 문구라 빼고 이 요약 정보로 교체함
- **마케팅 전략 상세**(L3 전용, `data-ob-lock` 대상): `16_마케팅전략상세`로 이동
- **우리 가게 스타일**(L1/L2 전용 미리보기, M10): 말투/강점 칩 일부만 미리 보여주고 [마케팅 전략 상세 보기 →]로 `16_마케팅전략상세`로 이동(문구가 그냥 "더보기"였던 걸 목적지를 명시하도록 바꿈) — AI가 분석한 기본값을 사장님이 확인하는 지점(§2 게이지의 "스타일 확인" 요소와 연결)

## 7. 함께 쓰는 사람

직원 초대·권한 관리 진입 카드. `20_함께쓰는사람`으로 이동. 캡션에 이용 중인 사람 이름을 최대 3명까지 나열하고("김하나, 박지훈 이용 중"), 4명이 넘으면 "OO, OO, OO 외 N명"으로 줄인다.

## 미해결 / 미구현

- **AI 이해도 게이지 계산 로직 없음** — `GAUGE.normal.width`는 여전히 하드코딩된 "92%"(100% 상태는 `GAUGE.complete`로 별도 데모 스위치가 있지만, 이 역시 고정값이지 실제 계산이 붙은 건 아님). 채널 연결 개수·매장 자료 등록 여부가 전부 화면별로 분리된 mock 데이터라(공유 상태 없음) 동적 계산을 붙이려면 화면 간 공유 데이터 구조가 먼저 필요(실제 백엔드 연동 시 자연히 해결)
- **표시값 92%는 재계산 안 된 예시값** — 실제로 재계산하면 다른 숫자가 나올 수 있음(현재 mock은 채널 3개 전부 연결돼 있어 100%에 가까워짐)
- **§5 검수 강도-모드 A/B 제약 로직 미구현** — 정책만 확정, 원본 HTML의 라디오 비활성화 로직은 아직 반영 안 함
- **§5 검수 대상 범위 불명확** — 이 화면 캡션은 "AI가 만든 **글**"(텍스트)을 지칭하는데 마케팅전략상세의 검수 강도는 "생성 **이미지**" 전용이라, 두 설정이 같은 대상을 가리키는지 범위가 불명확 — 확산 전에 재확인 필요
- **§3 대표 메뉴 업종별 프리셋 미설계** — "업종에 따라 대표메뉴 항목 자체가 달라져야 한다"는 방향만 정해졌고, 어떤 업종에 어떤 프리셋을 적용할지는 아직 미정. 현재는 모든 업종에 동일한 칩 입력 UI 노출
- **§3 수정 화면 저장 시 실제 반영 없음** — `06_우리매장정보_수정.html`의 [저장]은 값 검증/반영 없이 바로 `06_우리매장정보.html`로 돌아감(프로토타입 한계, §3 본문 참고)

## 변경 이력

- **AI 이해도 게이지 배점표 신설**: 이전엔 `normal`(92%)/`prelearn`(6%) 두 고정값만 있고 동적 계산이 없었는데, 이번에 §2의 배점표를 새로 정의함. "+8%" 배지 불일치는 배점표가 그 배지보다 나중에 정의돼서 생긴 것
- **검수 모드 설정 위치 이전**: 이전엔 `02_콘텐츠검토` 안의 숨은 모달이었다가 이 화면으로 옮김(§5)
- **§5 마케팅전략상세와의 관계, 방향 정정**: 처음엔 "이 화면(06)이 정본이고 마케팅전략상세는 읽기전용 표시"로 정리했으나, 마케팅전략상세의 모드B가 코드상 "사장님 승인 생략"이라 검수 강도(승인 필수)와 양립 불가능함을 확인해 방향을 정정함 — 마케팅전략상세 쪽이 제약의 원천이고, 이 화면의 선택지가 거기 맞춰 좁아지는 방향으로 바뀜
- **§3 매장 정보 수정 방식, 인라인 → 모달 → 전용 화면으로 재정정**: 처음엔 "인라인 편집 모드(B안)"로 확정했었으나, UI를 `98_매장설정.html` Step2와 통일하기로 하면서 먼저 모달(`#modal-edit-store`)로 구현함. 이후 필드가 10개가 넘어 모달 컨테이너와 안 맞는다는 문제(저장 버튼이 뷰포트 밖으로 밀림, 공통 `.uw-modal` CSS엔 overflow 처리가 없음)와 "화면이 98과 완전히 같지 않다"는 지적(요일별 설정 링크 누락 — 구현 중 임의로 뺐던 스코프였음)을 계기로, 모달을 걷어내고 `24_해지.html`과 같은 "별도 파일 + 브레드크럼 복귀" 전용 화면(`06_우리매장정보_수정.html`, UW_ST_10)으로 최종 전환함
- **§3 대표 메뉴, 텍스트 입력 → 칩(chip) 방식으로 전환**: 모달 작업 중 처음엔 대표메뉴를 textarea 자유 입력으로 추가했으나, "이름/가격을 하나씩 입력해 칩으로 노출"하는 방식이 이 화면의 기존 읽기 전용 표시(칩 형태)와 더 일치한다는 판단으로 전환. `98_매장설정.html`의 신규 등록 Step2 대표메뉴 섹션도 동일한 칩 UI로 함께 통일함
- **98_매장설정.html 사업자번호·이메일 필드 제거**: 매장 정보 수정 화면과 UI를 통일하는 과정에서 두 필드를 신규 등록 Step2에서도 함께 제거함(가입 시 이미 확보되는 정보로 판단)
- **게이지 100% 상태 정의 + AI 학습현황 배너 이동 + 매장 자료 요약 정보 신설(2026-09-02)**: 100%가 실제로 나올 수 있는 값인데도 화면에 대응하는 상태가 없었던 걸 채움(§2). AI 학습 현황 진입 카드(§6에 있던 것)를 게이지 배너 안 버튼으로 옮기고 마지막 학습일을 병기, 사이드바 별도 카드는 없앰. 매장 자료 카드는 배점표와 안 맞던 "+N%" 배지를 빼고 건수·최근 등록일 요약으로 교체(§6). 우리 가게 스타일의 "더보기" 버튼은 "마케팅 전략 상세 보기"로, 함께 쓰는 사람 캡션은 인원수만 있던 걸 이름 3명까지 노출하는 방식으로 각각 바꿈(§7)
- **AI 학습현황 진입, 버튼 → 텍스트 링크로 변경(2026-09-02)**: 바로 위 변경에서 `AI 학습현황`을 `.btn.btn-ghost.btn-sm`(36px)로 만들어 `+ 정보 더 채우기`(.btn.btn-primary, 48px) 옆에 뒀는데, 높이도 안 맞고 버튼끼리 위계가 부딪혀 보인다는 지적이 있었음. "연결된 채널" 카드의 `관리 →`와 같은 텍스트 링크 스타일로 바꾸고, 두 요소를 감싸는 행의 정렬도 상단(`flex-start`)에서 중앙(`center`)으로 바꿔 정리함
