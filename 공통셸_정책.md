# 공통 셸 정책

- 적용 대상: 사용자웹 전 화면
- 관련 자산: `assets/userweb.js`(레벨 렌더러), `assets/capability-matrix.js`(화면 × 모듈 × 레벨 정본), `assets/userweb.css`
- 검사기: `tools/verify-matrix.cjs`(정적), `tools/runtime-check.html`(런타임 39화면 × 3레벨)

## 배경

화면마다 LNB와 상단바를 각자 써 넣고 있어, 새 화면을 만들 때 일부를 빠뜨려도 그 화면을 열어보기 전까지 아무도 모른다. 실제로 `98_매장설정`은 L3 메뉴 그룹이 통째로 없었고(2026-09-03 발견), 레벨 전환 스위치도 없었다(2026-09-04 발견). 두 번 다 화면 안의 기능이 아니라 셸이 빠진 것이었다.

셸은 화면의 일부가 아니라 **모든 화면이 똑같이 갖춰야 하는 틀**이다. 이 문서는 그 틀의 구성과, 레벨에 따라 무엇이 어떻게 바뀌어야 하는지를 정한다.

## 1. 셸의 구성

| 부분 | 셀렉터 | 필수 여부 |
|---|---|---|
| LNB 메뉴 그룹 1 (주요 기능) | `nav.uw-menu` | 필수 |
| LNB 메뉴 그룹 2 (매장·도움말) | `nav.uw-menu` | 필수 |
| LNB 메뉴 그룹 3 (대행사) | `nav.uw-menu[data-only-level="L3"]` | 필수 |
| LNB 계정 블록 | `.uw-lnb-user` | 필수 |
| 상단바 제목·브레드크럼 | `.uw-topbar-title` | 필수 |
| 레벨 전환 스위치 | `.uw-lvl-switch` | 필수 |
| L3 담당 매장 수 | `.uw-lvl-note[data-only-level="L3"]` | 필수 |
| 매장 전환기 | `.uw-store-switch` | 필수 |
| 알림 벨 | `.uw-bell` | 필수 |

**레벨 전환 스위치는 프로토타입 전용이지만 빠지면 안 된다.** 이것이 없으면 그 화면의 레벨별 동작을 검증할 방법이 없어진다.

## 2. 셸을 갖추지 않는 화면

로그인 전 화면은 레벨 개념이 없으므로 셸을 두지 않는다.

`21_로그인` · `21_회원가입` · `19_초대가입` · `22_약관정책` · `10_온보딩` · `28_오류`

이 화면들은 자체 레이아웃(`.on-wrap`, `.err-wrap` 등)을 쓴다. 예외를 늘리려면 이 목록에 추가해야 하고, 목록에 없는 화면이 셸을 빠뜨린 것은 결함이다.

## 3. 레벨별 노출은 두 갈래로만 한다

| 방식 | 무엇 | 언제 |
|---|---|---|
| **모듈 게이팅** | HTML 에 `data-module` 을 붙이고 `capability-matrix.js` 의 `levels` 로 판정. `userweb.js` 의 `render()` 가 수행 | 기본. 화면 안 기능 블록 단위 |
| **속성 게이팅** | `data-only-level="L2,L3"` | 모듈이 아닌 것. 메뉴 항목, 안내 문구, 스텝 표시처럼 기능 블록이 아닌 요소 |

**화면 자체 스크립트로 레벨을 분기하지 않는다.** 그렇게 하면 매트릭스가 그 화면을 모르게 되고, 검사기와 IA 문서가 판정할 수 없다. 지금 `01_홈_미결제`가 이 방식이라 IA 표에서 수동 지정으로 남아 있다(`무료체험_요금제시작_정책.md` §4).

## 4. 화면 전체가 닫히는 레벨의 처리

한 화면의 모듈이 어떤 레벨에서 **전부** 닫히면 그 레벨에서는 빈 화면이 된다. 그대로 두지 않는다.

- 그 자리에 **대체 카드를 하나만** 띄운다. 모듈마다 `fallback` 을 달면 카드가 모듈 수만큼 뜬다
- **대표 모듈 하나에만** `fallback: "agency-card"` 와 `fallbackName` 을 단다. `fallbackName` 은 모듈명이 아니라 **화면 이름**으로 적는다 — 카드 문구가 "스텝1 네이버 플레이스 불러오기은 대행사가…"처럼 되면 안 된다
- 그 레벨에서 가리킬 대상이 없어지는 부속 요소(스텝 표시, 안내 문구)는 `data-only-level` 로 함께 숨긴다
- 메뉴에서도 숨길 화면이면 LNB 항목에 `data-only-level` 을 단다. **URL 직접 진입까지 막지는 않는다** — 들어오면 대체 카드를 보여준다

`fallback` 종류는 둘이다.

| 값 | 뜻 |
|---|---|
| `agency-card` | 대행사가 대신 수행하는 기능 |
| `tbd-card` | 요금제·결제 방식 확정 전이라 잠긴 자리 |

## 5. 셸 변경은 전 화면에 한 번에 한다

메뉴 항목을 더하거나 뺄 때 일부 화면만 고치면 화면마다 메뉴가 달라진다. 실제로 청구 화면군을 들여올 때 4화면에만 새 메뉴가 들어가 있었고, 나머지 28화면에 일괄로 넣어 맞췄다.

변경 후에는 `tools/verify-matrix.cjs` 의 LNB 그룹 일치 검사가 통과해야 한다.

## 6. 검사

| 검사기 | 무엇을 보나 |
|---|---|
| `tools/verify-matrix.cjs` | 화면 ↔ 매트릭스 정합 8종. LNB 그룹이 화면마다 같은지 포함 |
| `tools/runtime-check.html` | 39화면 × 3레벨. 콘솔 오류, 미포착 예외, 레벨 적용 일치, 괄호 조사 잔존, LNB 하단 잘림, 전 버튼 클릭 |

**레벨 전환 스위치 존재 여부는 아직 어느 검사기도 보지 않는다**(미해결 참조).

## 미해결 / 미구현

- **셸 필수 요소 검사가 없다.** `verify-matrix.cjs` 가 LNB 그룹 일치는 보지만 `.uw-lvl-switch`, `.uw-store-switch`, `.uw-bell` 의 존재는 보지 않는다. §2 의 예외 목록을 코드에 넣고 검사 항목을 추가해야 한다. 이번 두 결함이 모두 이 검사가 없어서 늦게 발견됐다
- **셸이 화면마다 복사돼 있다.** 근본 해법은 셸을 한 곳에서 그리는 것이지만, 이 목업은 화면별 단일 HTML 이 전제라(`html-to-figma-kit` 의 static 변환도 그것을 전제로 한다) 지금 구조에서는 검사로 막는 것이 현실적이다
- **`01_홈_미결제` 가 §3 을 위반하고 있다.** 화면 자체 스크립트(`.pay-pane`)로 레벨을 분기해 매트릭스가 판정하지 못한다. 모듈 게이팅으로 옮길지, 예외로 남길지 정하지 않았다
- **모듈이 없는 화면이 둘 있다.** `01_홈_미결제`(UW_HM_05), `06_우리매장정보_수정`(UW_ST_10). 후자는 단일 폼이라 쪼갤 모듈이 없다는 사유가 기록돼 있으나, 그 결과 레벨 판정을 못 한다
- **`data-module` 미부착 화면이 남아 있다.** `16_마케팅전략상세`(M01~M05), `16_유사업체비교`(M01~M03), `99_매장자료관리`(M01~M04), `02_발행이력`(M01), `10_온보딩`(M01·M05). 매트릭스에는 등재돼 있으나 HTML 에 속성이 없어 레벨 게이팅이 실제로는 걸리지 않는다

## 변경 이력

- **2026-09-04 신설**: `98_매장설정` 에서 레벨 전환 스위치가 안 보인다는 지적을 계기로 작성. 원인은 그 화면의 상단바에 `.uw-lvl-switch` 가 아예 없었던 것이고, 하루 전 같은 화면에서 LNB L3 그룹이 통째로 빠져 있던 것과 같은 뿌리였다. 개별 화면을 고치는 것으로 끝내지 않고 셸의 구성과 검사 방법을 규칙으로 남김
- **2026-09-04 `98_매장설정` 셸 표준화**: 상단바에 레벨 전환 스위치, L3 담당 매장 수, 매장 전환기를 넣고 알림 벨에 알림 센터 연결을 붙임. L1 에서는 모듈 4개가 전부 닫혀 빈 화면이 되던 것을 대체 카드 하나로 바꾸고(§4), 가리킬 대상이 없어진 스텝 표시와 안내 문구를 `data-only-level="L2,L3"` 로 숨김. 대체 카드가 스텝마다 뜨지 않도록 `fallback` 은 M01 에만 두고 `fallbackName` 을 화면 이름으로 유지함
