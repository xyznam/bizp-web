# 매장 목록 페이지 정책

- 관련 화면: `12_매장목록.html`(UW_ST_02), 연결: `98_매장설정.html`, `02_콘텐츠검토.html`, `17_대행사홈.html`
- 진입 경로: L3 LNB의 "🏬 매장 목록"(L3 전용 그룹). `13_계약관리`가 이 화면에 흡수·폐기됨(LNB에서도 계약관리 제거)

## 1. 접근 권한 — L3(대행사) 전용

L1(위탁)·L2(셀프)는 둘 다 같은 안내 카드로 막힘: "이 화면은 대행사 직원 전용이에요, 매장 목록은 담당 대행사 직원이 여러 매장을 관리할 때 쓰는 화면이에요" + [홈으로 가기]. 헤더 부제("담당 78곳 중 요주의부터 챙겨보세요")도 L3 전제로 고정.

## 2. 필터 바

요주의만(체크박스) / 검색(매장명·사장님·업종) / 운영상태(설정전·학습전·운영중) / 초대상태(초대 전·초대 대기·가입 완료) / 정렬(할 일 많은 순 기본값·이름순). 검색은 입력 즉시 반영(엔터 불필요), 나머지는 change 이벤트로 즉시 재렌더.

## 3. 테이블 컬럼과 배지 체계

| 컬럼 | 의미 |
|---|---|
| 매장 | 매장명 + 브랜드 배지(공식 브랜드면 보라 배지로 브랜드명, 검증대기면 회색 배지로 "브랜드명 · 검증대기") |
| 업종 | 텍스트 |
| 사장님 | 초대 전이면 "-"로 표시(아직 사장님 정보 자체가 없음) |
| 운영상태 | 설정전(파랑)/학습전(주황)/운영중(초록) — `매장구조_회원정책.md`의 콘텐츠 상태 3단계와 동일 개념 |
| 활성 | on/off 토글(별도 항목, §8 참조) |
| 초대상태 | 초대 전(주황)/초대 대기(보라)/가입 완료(회색) |
| 오늘 할 일 | 건수(0이면 "—") |
| 요주의 | 있을 때만 배지(예: "수집 실패"), 없으면 "—" |
| 상세 | [상세] 버튼 → 팝업 |

캡션에 "표본 N곳 중 M곳 표시 · 전체 78곳은 검색으로 찾아요"라고 명시 — 이 화면의 mock은 5개 매장뿐이고 실제 78곳 전량 목업이 아님을 화면 자체가 밝히고 있음(설계 의도).

## 4. 운영상태(설정전/학습전/운영중) vs 매장 활성 상태

`매장구조_회원정책.md`는 매장 활성 상태를 이용중/이용중(무료)/이용중(트라이얼)/미결제 정지/관리자 정지/휴면/폐업, 7가지로 정의한다. 이 화면의 "활성" 토글(on/off)은 그중 **관리자 정지 하나만 조작**하는 수동 스위치다 — 대행사 직원이 이 토글을 끄면 "관리자 정지" 상태로 전환, 다시 켜면 "이용중"으로 복귀. **나머지 상태(이용중(무료)·이용중(트라이얼)·미결제 정지·휴면·폐업)는 토글로 조작할 수 없고, 상태만 읽기전용으로 표시한다** — 시스템이 자동으로 전환하는 영역이라 대행사가 임의로 못 바꾸는 게 맞기 때문. 화면 구현 방향: 활성 토글 옆(또는 대체)에 이 상태들을 나타내는 배지를 추가하고, 이 배지가 켜져 있는 매장은 활성 토글 자체를 비활성화(조작 불가) 처리해야 함 — 예: "미결제 정지" 배지가 떠 있으면 토글을 만져도 안 바뀜, 결제가 되거나 관리자가 직접 처리해야 풀림.

## 5. 매장 상세 팝업

기본 정보(업종/브랜드/사업자번호/요금제/운영상태/초대상태) + 조건부 정보:
- **사장님/휴대폰 행**: 초대상태가 "가입 완료"일 때만 노출(그 전엔 사장님 정보 자체가 없거나 미확정이므로 숨김)
- **[매장설정] 버튼**: 운영상태가 "설정전"일 때만 노출 → `98_매장설정.html`로 이동
- **[초대정보] 버튼**: 초대상태가 "초대 대기"일 때만 노출(재확인·재발송용) → 초대 모달 재오픈
- **[초대 보내기] 버튼**: 초대상태가 "초대 전"일 때만 노출 → 초대 모달
- **[수정] 버튼**: 항상 노출

## 6. 매장 정보 수정

매장 이름/사장님/휴대폰/요금제 수정 가능. **사장님·휴대폰은 초대상태가 "가입 완료"면 입력이 잠김**("가입 완료된 사장님 정보는 여기서 수정할 수 없어요, 변경이 필요하면 고객센터에 문의해 주세요") — 본인 인증을 거쳐 가입한 사장님 정보를 대행사가 임의로 못 바꾸게 하는 안전장치. 사업자번호·업종은 아예 수정 불가(안내 문구만, "어드민에 문의").

## 7. 신규 매장 등록 (사업자번호 4케이스 + 브랜드 3종)

**사업자번호 확인 결과 4케이스** (mock: `1111111111`/`2222222222`/`3333333333`는 각각 아래 3케이스를 시연, 그 외 숫자는 신규):
1. **같은 대행사가 이미 담당** → 자동으로 같은 사장님 계정에 새 지점으로 연결(등록 진행 가능)
2. **다른 대행사가 이미 담당** → 차단, "고객센터에 문의"만 노출(등록 진행 불가)
3. **이미 비즈플래닛을 직접 이용 중인 사장님**(셀프/L2) → 차단, "고객센터에 문의"(등록 진행 불가) — 대행사가 임의로 셀프 이용 매장을 가로채 등록하지 못하게 막음
4. **신규 사업자번호** → 정상 등록 진행

**브랜드 3종** (신규 매장 등록 시 선택):
- **브랜드 없음**: 독립 매장(기본값)
- **공식 브랜드 검색**: 기존에 등록된 프랜차이즈 브랜드 중 자동완성으로 선택(예: BBQ치킨, 이디야 등 mock 20개) → `brandStatus: "official"`
- **신규 브랜드 등록**: 목록에 없는 브랜드명 직접 입력 → `brandStatus: "pending"`(어드민 검증 전까지는 "검증대기" 배지로 노출되지만 **매장 등록·운영 자체는 정상 진행**, 검증 대기가 운영을 막지 않음)

신규 매장은 항상 운영상태 "설정전"으로 시작. 등록 완료 시 사업자번호가 케이스1(같은 대행사 기존 사장님)이면 사장님 정보가 자동 연결돼 초대상태가 바로 "가입 완료"로 시작하고, 그 외엔 "초대 전"으로 시작(별도로 초대를 보내야 함).

**[정의만, 화면 미구현]**: 프랜차이즈 본사가 자사 브랜드의 신규 지점을 등록하는 화면은 이 화면과 다르게, "자사 소유 브랜드 검색"으로 브랜드 필드가 한정되고(더본코리아처럼 본사가 여러 브랜드를 소유할 수 있음) 신규 브랜드 직접 입력 옵션이 없어야 함. 별도 화면 필요, 아직 미구현.

## 8. 초대 보내기 (3케이스)

이름+휴대폰 입력 후 [확인]:
1. **LINKED**(이름·번호가 기존 가입 사장님과 정확히 일치) → 초대 문자 없이 바로 계정 연결, 버튼 라벨 "계정에 연결하기"
2. **PHONE_MISMATCH**(번호는 기존 가입자와 같은데 이름이 다름) → 차단, "고객센터에 문의"(진행 불가) — 번호 재사용/오타로 다른 사람 계정에 잘못 연결되는 사고 방지
3. **NEW**(번호가 등록된 적 없음) → 정상 진행, 버튼 라벨 "초대 보내기"(문자 발송)

## 9. 매장 활성/비활성 토글

- **재활성화(off→on)**: 확인 없이 즉시 반영
- **비활성화(on→off)**: 확인 모달을 거침("비활성화 시 해당 매장의 모든 서비스가 즉시 중단돼요" 경고 + 내부 메모 입력칸(사장님에겐 안 보임)). 확정 전까지 토글 스위치는 켜진 상태로 되돌아가 있음(모달에서 취소하면 그대로 유지)

## 10. 일괄 액션 (체크박스 선택)

선택 시에만 활성화되는 2개 버튼: [다시 학습](토스트만, 실제 재학습 트리거 미구현) / [검토 큐 열기](`02_콘텐츠검토.html`로 이동, 선택 매장 필터링은 미구현 — 그냥 이동만 함). 미선택 시 두 버튼 다 비활성.

## 11. 행 클릭 = 매장 컨텍스트 전환

테이블 행(상세 버튼·체크박스·토글 셀 제외 영역) 클릭 시 헤더의 매장 스위처(`🏪 매장 ▾`)가 그 매장명으로 바뀌고 토스트 노출 — 이 화면에서 다른 화면으로 넘어갈 때 "지금 보고 있는 매장" 컨텍스트를 바꾸는 용도로 보임(다만 실제로 다른 화면 이동 시 이 선택이 유지되는지는 미확인 — 새로고침하면 사라지는 세션 변수라 확산 시 재검증 필요).

## 12. 미해결/미구현

- §4 정책은 확정됐으나 화면(HTML)에는 아직 미반영 — 지금 mock 5개 매장은 전부 이용중/설정전/학습전뿐이라 미결제 정지·휴면·폐업 배지·토글 잠금 상태를 보여줄 예시 데이터도 없음
- 일괄 [다시 학습] 실제 트리거 없음(토스트만)
- 일괄 [검토 큐 열기]가 선택 매장 기준으로 필터링되지 않음(그냥 화면 이동)
- 행 클릭으로 바뀐 매장 컨텍스트가 다른 화면까지 이어지는지 미검증
- 프랜차이즈 본사 전용 신규 매장(지점) 등록 화면 미구현(§7 하단 참조)

## 변경 이력

- **§4 활성 토글의 의미 확정**: 매장 활성 상태 7가지(매장구조_회원정책.md) 중 이 화면의 on/off 토글이 정확히 무엇을 조작하는지 정의돼 있지 않았는데, "토글은 관리자 정지만 조작하고 나머지 상태(이용중(무료)·이용중(트라이얼)·미결제 정지·휴면·폐업)는 읽기전용으로만 표시한다"로 확정함 — 시스템이 자동으로 전환하는 상태를 대행사가 임의로 건드리지 못하게 하려는 목적
