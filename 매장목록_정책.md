# 매장 목록 페이지 정책

- 관련 화면: `12_매장목록.html`(UW_ST_02), 연결: `98_매장설정.html`, `17_대행사홈.html`
- 진입 경로: L3 LNB의 "🏬 매장 목록"(L3 전용 그룹). `13_계약관리`가 이 화면에 흡수·폐기됨(LNB에서도 계약관리 제거)

## 1. 접근 권한 — L3(대행사) 전용

L1(위탁)·L2(셀프)는 안내 카드 + 홈 이동 버튼으로 막힘. 헤더 부제도 L3 전제로 고정.

## 2. 필터 바

요주의만(체크박스) / 검색(매장명·사장님·업종) / 운영상태(설정전·학습전·운영중) / 초대상태(초대 전·초대 대기·가입 완료) / 정렬(할 일 많은 순 기본값·이름순). 검색은 입력 즉시 반영(엔터 불필요), 나머지는 change 이벤트로 즉시 재렌더.

## 3. 테이블 컬럼과 배지 체계

| 컬럼 | 의미 |
|---|---|
| 매장 | 매장명 + 브랜드 배지(공식 브랜드=보라 배지로 브랜드명, 검증대기=회색 배지) |
| 업종 | 텍스트 |
| 사장님 | 초대 전이면 미표시(정보 자체가 없음). 초대받은 사람이 매니저면 이름 옆에 매니저 배지 추가(§8) |
| 운영상태 | 설정전(파랑)/학습전(주황)/운영중(초록) — `매장구조_회원정책.md`의 콘텐츠 상태 3단계와 동일 개념 |
| 활성 | on/off 토글(§9) |
| 초대상태 | 초대 전(주황)/초대 대기(보라)/가입 완료(회색) |
| 오늘 할 일 | 건수(0이면 미표시) |
| 요주의 | 있을 때만 배지, 없으면 미표시 |
| 상세 | 상세 팝업 진입 |

캡션에 현재 페이지 표시 범위 + 필터 적용 후 전체 건수 + 실제 담당 매장 수(78곳)를 함께 안내 — mock은 25개 매장뿐이라 78곳 전량을 담은 목업이 아님을 화면 스스로 밝힘(설계 의도). 표시 개수는 §11의 페이지네이션에 따라 달라짐.

## 4. 운영상태(설정전/학습전/운영중) vs 매장 활성 상태

매장 활성 상태 7가지(`매장구조_회원정책.md`): 이용중 / 이용중(무료) / 이용중(트라이얼) / 미결제 정지 / 이용 중지 / 휴면 / 폐업

- **활성 토글(on/off)이 조작하는 범위**: 이 중 **이용 중지 하나뿐**. 끄면 이용 중지(대행사 처리)로 전환, 켜면 이용중으로 복귀
- **나머지 6가지**: 토글로 조작 불가, 읽기전용 표시만 — 시스템이 자동 전환하는 영역이라 대행사가 임의로 못 바꾸는 게 맞기 때문
- **구현 방향(미반영)**: 이 상태들을 배지로 노출하고, 배지가 떠 있는 매장은 토글 자체를 조작 불가 처리(결제 완료·관리자 처리 등으로만 해제)

## 5. 매장 상세 팝업

- **비활성화 사유 메모(최상단)**: §9에서 남긴 메모가 있으면 팝업 맨 위에 빨간 경고 배너로 노출(제목 바로 아래, 다른 정보보다 먼저 보여야 하는 항목이라 상단 고정), 없으면 배너 자체가 숨겨짐
- 그 아래 기본 정보(업종/브랜드/사업자번호/요금제) + 조건부 정보:
  - **사장님/매니저 · 휴대폰 행**: 초대상태가 가입 완료일 때만 노출. **대표가 있으면 항상 대표를 우선 노출**하고, 매니저는 대표가 없을 때만 그 자리를 대신 채우는 표시일 뿐이다 — 매니저가 있는 매장에 나중에 대표가 합류하면(`20_함께쓰는사람.html`에서 초대·수락) 이 행은 매니저 대신 대표로 갈아끼워진다(매니저가 팀에서 빠지는 건 아님, 이 한 줄 요약 표시만 대표 쪽으로 바뀜)
  - **대표 미가입 안내**: 대표가 아직 없는 매장(매니저만 있음)은 위 행 대신 안내 문구 노출. 대표 초대는 이 화면이 아니라 `20_함께쓰는사람.html`(매장 자체 화면) 몫이라 이 팝업엔 안내만 있고 액션은 없음 — 대표 없는 매장의 매니저가 그 화면에서 직접 사장님을 초대할 수 있다(`함께쓰는사람_정책.md` §1)
- **하단 배지 3종**: 매장상태(§4) / 운영상태 / 초대상태
  - 매장상태는 §4의 활성 토글을 그대로 반영 — 이용중(초록) / 이용 중지(빨강) 둘 중 하나만 표시(나머지 5가지 상태는 아직 배지로 안 뽑음, §14)

**버튼 노출 규칙** — 운영상태·매장상태·초대상태 3개 축 중, 실제로 버튼 노출을 가르는 건 운영상태·초대상태뿐이다(매장상태는 배지로만 쓰이고 버튼 노출엔 관여 안 함):

| 버튼 | 운영상태 조건 | 매장상태 조건 | 초대상태 조건 | 클릭 시 |
|---|---|---|---|---|
| 초대 보내기 | 무관 | 무관 | 초대 전 | 초대 모달(신규 초대) |
| 초대정보 | 무관 | 무관 | 초대 대기 | 초대 모달 재오픈(재확인·재발송용, §8) |
| 매장설정 | 설정전 | 무관 | 무관 | `98_매장설정.html`로 이동 |
| 매장 삭제 | 설정전 | 무관 | 무관 | 삭제 확인 모달(§10) |
| 고객센터 문의 안내 | 설정전이 아님 | 무관 | 무관 | 액션 없음(안내 문구만, 삭제 버튼 자리를 대신함) |
| 수정 | 항상 숨김 | 항상 숨김 | 항상 숨김 | 잠정 비활성화(§6) |
| 닫기 | 무관 | 무관 | 무관 | 팝업 닫기(항상 노출) |

## 6. 매장 정보 수정 [잠정 비활성화]

수정 버튼을 화면에서 뺐다(모달은 코드에 남아있으나 진입로 없음). 재설계 방향 미정 — §14 미해결 참조.

- 사업자번호·업종은 어차피 수정 대상이 아니었음(다른 화면·시스템의 기준값이라 어드민 경유만 허용)

## 7. 신규 매장 등록 (사업자번호 3케이스 + 브랜드 3종)

**사업자번호 확인 결과 3케이스** — 초대 보내기(§8)와 같은 정책·구조: 이미 존재하는 번호로 확인되면 자동 연결하지 않고 항상 차단, 재확인을 요구한다. 실제로 등록이 진행되는 건 신규뿐:
1. 우리 대행사가 이미 담당 → 차단, 이미 등록된 매장임을 이름으로 특정해 안내(번호 재확인 요청) — 사업자번호는 매장(지점)마다 고유하므로, 기존 번호와 일치하면 새 지점이 아니라 중복 입력일 가능성이 높음
2. 우리 대행사가 아닌 곳에 이미 등록(다른 대행사 소속이든 직접 이용 중이든 구분하지 않음) → 차단, 고객센터 문의로만 유도 — 대행사가 임의로 남의 매장을 가로채 등록하지 못하게 막음
3. 신규 사업자번호 → 정상 등록 진행

**브랜드 3종**:
- 브랜드 없음: 독립 매장(기본값)
- 공식 브랜드 검색: 기존 등록 프랜차이즈 브랜드 자동완성 선택 → `brandStatus: "official"`
- 신규 브랜드 등록: 목록에 없는 브랜드명 직접 입력 → `brandStatus: "pending"`(어드민 검증 전까지 검증대기 배지, **등록·운영 자체는 정상 진행**)

- 신규 매장은 항상 **운영상태 설정전, 초대상태 초대 전, 역할 대표**로 시작(신규 사업자번호 케이스만 등록에 도달하므로 사장님 정보는 아직 없음)
- 이 등록 플로우엔 역할 선택이 없음 — 매니저 초대는 §8의 개별 초대 플로우 전용

**[정의만, 화면 미구현]**: 프랜차이즈 본사가 자사 브랜드의 신규 지점을 등록하는 화면은 이 화면과 다르게, 브랜드 필드가 "자사 소유 브랜드"로 한정되고(본사가 여러 브랜드를 소유할 수 있음) 신규 브랜드 직접 입력 옵션이 없어야 함. 별도 화면 필요, 아직 미구현.

## 8. 초대 보내기 (역할 선택 + 검증 3케이스)

매장 대표가 직접 관리하지 않고 직원이 관리하는 경우를 위한 기능:

- 초대 전 **역할을 먼저 선택**: 사장님(대표) / 매니저(직원)
- 매니저로 초대하면 이 매장은 대표가 없는 매장이 되고, 목록·상세 어디서나 매니저 배지가 붙음(§3, §5)
- 매니저만 있는 매장에서 대표를 나중에 초대하는 건 별도 화면(`20_함께쓰는사람.html`) 몫 — 이 화면 범위 밖

역할 선택 후 이름+휴대폰 입력, 확인을 누르면 번호를 검증한다. **이미 가입된 번호는 역할과 무관하게 초대 자체가 불가능하다**(이름·번호 일치 시 자동 계정 연결하던 옛 케이스는 폐지):

| 케이스 | 판정 근거 | 결과 |
|---|---|---|
| 신규 번호 | 우리 대행사 소속에도, 외부 가입자에도 없음 | 진행 가능 |
| 우리 대행사에서 이미 위탁 운영 중인 사장님 번호 | 자체 관리 중인 사장님이라 이름까지 알고 있음 | 차단, 이름을 특정해 안내 |
| 그 외 이미 가입한 회원 | 다른 대행사 소속 또는 직접 가입 — 이름은 조회 권한이 없어 알 수 없음 | 차단, 일반화된 안내만 |

## 9. 매장 활성/비활성 토글

- **재활성화(off→on)**: 확인 없이 즉시 반영
- **비활성화(on→off)**: 경고 + 내부 메모 입력(사장님 비노출)이 담긴 확인 모달을 거침. 확정 전까지 토글은 켜진 상태로 되돌아가 있음(취소 시 유지). 입력한 메모는 저장되어 §5의 상세 팝업에 노출됨

## 10. 매장 삭제 (설정전 전용)

- **설정전인 매장만** 상세 팝업에서 직접 삭제 가능 — 아직 콘텐츠·결제·초대 등 얽힌 게 없어 되돌릴 것도 없기 때문. 확인 모달을 거쳐 확정하면 목록에서 즉시 제거됨
- **설정전이 아닌 매장은 삭제 버튼 자체가 없고 고객센터 문의 안내로 대체** — 이미 세팅이 끝난 매장은 결제·콘텐츠·스태프 등이 얽혀 있어 대행사가 임의로 삭제하지 못하게 막는 것

## 11. 페이지네이션

- **한 페이지 10개씩** 표시. 결과가 10개 이하면 페이지 버튼 자체가 사라짐
- 검색어·필터·정렬을 바꾸면 항상 **1페이지로 리셋** — 필터링된 결과가 지금 보던 페이지보다 적어질 수 있어서
- 전체 선택 체크박스는 **현재 페이지에 보이는 매장만** 선택/해제(필터링된 전체 목록이 아님) — 화면에 안 보이는 매장까지 한 번에 선택돼버리는 사고를 막기 위함
- 이미 선택해 둔 항목은 페이지를 넘겨도 유지됨

## 12. 일괄 액션 (체크박스 선택)

선택 시에만 활성화되는 버튼: 다시 학습(토스트만, 실제 재학습 트리거 미구현). 미선택 시 비활성.

## 13. 행 클릭 = 매장 컨텍스트 전환

- 테이블 행(상세 버튼·체크박스·토글 셀 제외) 클릭 시 헤더의 매장 스위처가 그 매장명으로 바뀜(토스트로만 시뮬레이션)
- 용도 추정: 다른 화면으로 넘어갈 때 "지금 보고 있는 매장" 컨텍스트를 바꾸는 것으로 보임
- 미검증: 다른 화면 이동 시 이 선택이 유지되는지 미확인 — 새로고침하면 사라지는 세션 변수라 재검증 필요

## 14. 미해결/미구현

- §4의 7가지 매장 활성 상태 중 이용중/이용 중지 2가지만 상세 팝업 매장상태 배지로 반영됨 — 나머지 5가지(이용중(무료)·이용중(트라이얼)·미결제 정지·휴면·폐업)는 아직 배지도, 토글 잠금 상태도 없음(mock 25곳 전부 이용중/이용 중지뿐이라 보여줄 예시 데이터도 없음)
- 일괄 다시 학습 실제 트리거 없음(토스트만)
- 행 클릭으로 바뀐 매장 컨텍스트가 다른 화면까지 이어지는지 미검증
- 프랜차이즈 본사 전용 신규 매장(지점) 등록 화면 미구현(§7)
- **매장 정보 수정 재설계 미정(§6)**: 요금제는 다른 화면(요금제변경)으로 이관됐고, 사장님/휴대폰은 초대정보(검증 포함)와 중복이라 버튼을 껐다. 남는 매장 이름 하나를 위해 지금 같은 모달을 유지할지, 더 가벼운 방식으로 바꿀지 논의 중

## 변경 이력

- **§4 활성 토글의 의미 확정**: 매장 활성 상태 7가지(매장구조_회원정책.md) 중 이 화면의 on/off 토글이 정확히 무엇을 조작하는지 정의돼 있지 않았는데, 토글은 이용 중지만 조작하고 나머지 상태는 읽기전용으로만 표시하는 걸로 확정함 — 시스템이 자동으로 전환하는 상태를 대행사가 임의로 건드리지 못하게 하려는 목적
- **초대 보내기 역할 선택·검증 로직 전면 개편, 비활성화 메모 노출, 설정전 매장 삭제 추가**: 초대 시 사장님/매니저 역할을 선택하게 하고, 이름+번호 일치로 자동 연결하던 케이스를 폐지해 이미 가입된 번호는 소속(우리 대행사/그 외)에 따라 항상 차단하도록 바꿈. 비활성화 시 남기는 사유 메모가 어디에도 노출되지 않던 문제를 찾아 상세 팝업에 표시하도록 수정. 설정전 매장에 한해 삭제를 허용하고, 설정 후 매장은 고객센터 문의로 유도. 사용 빈도가 낮다고 판단해 일괄 액션의 검토 큐 열기 버튼은 제거함
- **페이지네이션 도입(1페이지 10개)**: 표를 10개 단위로 자르고 이전/다음·페이지 번호 버튼을 추가함. 페이지네이션을 실제로 시연할 수 있도록 mock 매장을 5곳→25곳으로 늘림(§3, §14 데이터 규모 서술도 함께 갱신)
- **신규 매장 등록 케이스1(같은 대행사 기존 사업자번호) 자동 연결 폐지**: 사업자번호가 일치하면 확인 없이 같은 사장님 계정에 새 지점으로 자동 연결하던 걸, 초대 보내기(§8)와 동일하게 차단 + 이미 등록된 매장을 이름으로 특정해 재확인 요구하는 걸로 바꿈. 사업자번호는 매장마다 고유하다는 전제라 기존 번호와 일치하면 새 지점이 아니라 중복 입력일 가능성이 높다고 판단. 이 변경으로 등록이 실제로 진행되는 건 신규 사업자번호 케이스뿐이 됨
- **다른 대행사 담당/직접 이용 중 케이스 통합**: 위 변경 직후, 우리 대행사 소속이 아닌 사업자번호는 다른 대행사 소속이든 셀프 이용 중이든 구분할 실익이 없다고 판단해 하나로 합침. 4케이스(같은 대행사/다른 대행사/직접 이용/신규) → 3케이스(우리 대행사/그 외/신규)로 단순화, 초대 보내기(§8)의 3케이스 구조와 대칭이 맞음
- **수정 버튼 잠정 비활성화(2026-09-02)**: 원래 매장 이름/사장님/휴대폰/요금제 4개를 고칠 수 있었는데, 요금제는 요금제변경 화면으로 옮기기로 했고, 사장님/휴대폰은 검증 없이 덮어쓰는 반면 같은 일을 하는 초대정보(§5) 쪽엔 이름·번호 검증이 있어 중복이자 더 약한 경로였음이 확인됨. 남는 매장 이름 하나만으로 지금 모달을 유지할지 결론 못 내려 일단 버튼만 껐다(모달·JS는 코드에 남겨둠, 재설계는 별도 논의)
- **비활성화 사유 상단 배치 + 매장상태 배지 신설**: 비활성화 사유 메모가 다른 정보들 사이에 묻혀 있던 걸 팝업 최상단 빨간 경고 배너로 옮김. 하단 배지 줄에 매장상태(§4의 이용중/이용 중지)를 새로 추가 — §4에서 정의만 해두고 화면엔 반영 안 하고 있던 걸 일부(7가지 중 2가지) 구현
- **매니저만 있는 매장의 대표 초대 기능 구현(2026-09-02)**: §5·§14에 "함께쓰는 사람 메뉴 예정"으로만 적혀 있던 게 실제로 `20_함께쓰는사람.html`에 구현됨 — 대표가 없는 매장에서 매니저가 그 화면의 초대 모달로 직접 사장님을 초대할 수 있다. 이 화면(12) 쪽은 안내 문구만 갱신, 실제 구현·정책은 `함께쓰는사람_정책.md` §1
- **사장님/매니저 요약 행, 대표 우선 노출 규칙 명문화(2026-09-02)**: 매니저만 있던 매장에 나중에 대표가 합류하면 이 행이 매니저에서 대표로 전환돼야 한다는 점을 명시. 기존엔 "역할에 따라 라벨 전환"이라고만 적혀 있어 이미 매니저가 표시 중인 상태에서 대표가 나중에 추가되는 경우까지 명확히 커버하는지 애매했음
